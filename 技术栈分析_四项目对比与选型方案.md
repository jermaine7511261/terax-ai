# 技术栈深度分析 — 四项目对比与融合选型方案

## 一、语言生态对比

| 维度 | Terax AI | Grok Build | OpenCode | Hermes Agent |
|------|:--------:|:----------:|:--------:|:-----------:|
| **主语言** | TypeScript + Rust | **Rust 100%** | **TypeScript 100%** | **Python** + TS + Rust |
| **TS 版本** | ~6.0.3 | — | 5.8.2 | ^6.0.3 |
| **Rust 版本** | 最新 (Tauri 2) | **1.82.0** (edition 2024) | — | 微量 (FTS5) |
| **Python 版本** | — | — | — | **>=3.11,<3.14** |
| **Node 版本** | 20+ | — | Bun 1.3 | 20+ / 22 |
| **样式语言** | Tailwind CSS v4 | — | Tailwind CSS v4 | Tailwind CSS v4 |
| **包管理** | pnpm | Cargo | **Bun** | **uv (Python)** + npm (JS) |

### 1.1 语言选择对比分析

```
                    性能                   
                      │
          Rust (Grok) │  Rust+Tauri (Terax)
                      │
                      ├────────────────── 开发效率
                      │
        Hermes(Python)│  TS (OpenCode)
                      │
```

| 项目 | 选择理由 | 代价 |
|------|---------|------|
| **Terax** (Rust + TS) | Tauri 需要 Rust 后端 + TS/React 前端 | 双语言心智负担，IPC 桥接复杂度 |
| **Grok** (纯 Rust) | 极致性能 + 跨平台二进制 | 开发效率低，UI 开发痛苦 |
| **OpenCode** (纯 TS) | 最大开发效率 + 生态丰富 | 性能受限，无原生桌面能力 |
| **Hermes** (Python + TS) | AI 生态最丰富(Python) + Electron 桌面 | Python 性能瓶颈，包体积大 |

### 1.2 推荐选型

```
┌──────────────────────────────────────────────┐
│         Terax-Super 技术栈决策                │
├──────────────────────────────────────────────┤
│                                              │
│  Rust (后端/Tauri) ◄────── 继承 Terax        │
│    ├── PTY / Git / FS / Net / Secret         │
│    ├── + Memory (FTS5) / Sandbox / Checkpoint │
│    └── + MCP Client / ACP Server             │
│                                              │
│  TypeScript + React 19 (前端) ◄── 继承 Terax │
│    ├── UI / Composer / Editor / Terminal      │
│    ├── + Agent Runtime (双Agent + @mention)   │
│    └── + LSP Bridge / Provider Router         │
│                                              │
│  ❌ 不采用 Python (Hermes 核心需 Rust 重写)   │
│  ❌ 不采用 Bun (Terax 已有 pnpm 生态)          │
│  ❌ 不采用 Effect-ts (OpenCode 架构)           │
└──────────────────────────────────────────────┘
```

**核心原则**: 保持 Terax 的 Rust + TS 双语言架构，Hermes 的 Python 逻辑用 Rust 重写。

---

## 二、前端框架对比

| 维度 | Terax AI | Grok Build | OpenCode | Hermes Agent |
|------|:--------:|:----------:|:--------:|:-----------:|
| **UI 框架** | **React 19** | ratatui (Rust) | **SolidJS 1.9** | **React 19** |
| **构建工具** | **Vite 8** | Cargo | Vite 7 | **Vite 8** |
| **样式** | **Tailwind v4** | — | Tailwind v4 | **Tailwind v4** |
| **组件库** | **shadcn/ui** + Radix | — | Kobalte | shadcn/ui + Radix |
| **状态管理** | **Zustand 5** | Rust 自有 | — | nanostores + react-query |
| **包管理** | **pnpm** | Cargo | Bun | npm |
| **代码规范** | **Biome** | clippy+rustfmt | Oxlint | ESLint v9 + Prettier |
| **测试** | **Vitest** | cargo test | — | Vitest |
| **终端 UI** | **xterm.js** | ratatui | @opentui/solid | **Ink 6 (React CLI)** |

### 2.1 前端架构对比

```
Terax AI:       React 19 + Vite 8 + Taiilwind v4 + shadcn/ui + Zustand
                ← 经典全功能前端栈 →

OpenCode:       SolidJS 1.9 + Vite 7 + Tailwind v4 + Kobalte
                ← 更轻量但生态较小 →

Hermes Desktop:  React 19 + Vite 8 + Tailwind v4 + shadcn/ui + nanostores
                ← 与 Terax 高度一致 →
                
Hermes TUI:     Ink 6 (React for CLI)
                ← 独特的 React 终端渲染 →

Grok Build:     ratatui 0.29 (纯 Rust TUI)
                ← 完全不同的范式 →
```

### 2.2 前端选型推荐

```
┌──────────────────────────────────────────────┐
│          前端技术栈决策                        │
├──────────────────────────────────────────────┤
│                                              │
│  ✅ React 19         ─── 继承 Terax + Hermes │
│  ✅ Vite 8           ─── 继承 Terax          │
│  ✅ Tailwind CSS v4  ─── 继承 Terax + Hermes │
│  ✅ shadcn/ui + Radix ─── 继承 Terax         │
│  ✅ Zustand 5         ─── 继承 Terax         │
│  ✅ Biome             ─── 继承 Terax          │
│  ✅ Vitest            ─── 继承 Terax          │
│  ✅ xterm.js + WebGL  ─── 继承 Terax          │
│  ✅ CodeMirror 6      ─── 继承 Terax          │
│                                              │
│  ❌ SolidJS           ─── OpenCode 生态太小   │
│  ❌ ratatui           ─── 与 Tauri 架构冲突   │
│  ❌ nanostores        ─── 不如 Zustand 成熟   │
│  ❌ Oxlint            ─── Biome 已足够        │
└──────────────────────────────────────────────┘
```

**结论**: 前端完全继承 Terax 的技术栈。Hermes 的 Electron 桌面部分（React 19 + Vite 8 + Tailwind v4）与 Terax 高度一致，代码可复用。

---

## 三、后端框架对比

| 维度 | Terax AI | Grok Build | OpenCode | Hermes Agent |
|------|:--------:|:----------:|:--------:|:-----------:|
| **主语言** | **Rust** | Rust | **TypeScript** | Python |
| **桌面框架** | **Tauri 2** | — | — | **Electron 40** |
| **异步运行时** | **Tokio** | Tokio | — | asyncio |
| **HTTP 服务** | Tauri IPC | **Axum 0.8** | Hono 4 | FastAPI |
| **序列化** | Serde | **Serde + Protobuf** | Zod | Pydantic |
| **终端** | **portable-pty** | **portable-pty** + alacritty | node-pty | — |
| **LSP** | 自制 JSON-RPC | **async-lsp** | **vscode-jsonrpc** | — |
| **Git** | **自制(CLI)** | **git2/libgit2** | — | — |
| **SQLite** | — | **rusqlite + sqlite-vec** | **Drizzle ORM** | **sqlite3 (std lib)** |
| **日志/追踪** | — | **fastrace + OpenTelemetry** | — | — |
| **测试** | — | **criterion + insta** | Effect Test | **pytest** |
| **沙箱** | — | **nono + bwrap** | — | Docker 隔离 |

### 3.1 后端架构对比

```
Terax AI Tauri 架构:
┌──────────────────────────────────────────────┐
│                 Tauri 2 进程                    │
│  ┌──────────────┐     IPC      ┌──────────┐  │
│  │  Rust Backend │ ◄────────► │  React   │  │
│  │  (Tokio)     │  (Channel)  │  Frontend│  │
│  └──────────────┘             └──────────┘  │
└──────────────────────────────────────────────┘

Grok Build 架构:
┌──────────────────────────────────────────────┐
│         单进程 Rust 应用 (Tokio)               │
│  ┌──────────┐ ┌──────────┐ ┌──────────────┐  │
│  │ Agent    │ │ ratatui  │ │ Tools        │  │
│  │ Runtime  │ │ TUI      │ │ (gRPC+HTTP)  │  │
│  └──────────┘ └──────────┘ └──────────────┘  │
└──────────────────────────────────────────────┘

OpenCode 架构:
┌──────────────────────────────────────────────┐
│          Bun/Node.js 进程                      │
│  ┌──────────┐ ┌──────────┐ ┌──────────────┐  │
│  │ CLI      │ │ TUI      │ │ Server       │  │
│  │ (yargs)  │ │ (SolidJS)│ │ (Hono HTTP)  │  │
│  └──────────┘ └──────────┘ └──────────────┘  │
└──────────────────────────────────────────────┘

Hermes Agent 架构:
┌──────────────────────────────────────────────┐
│            多进程 Python + Node               │
│  ┌──────────┐ ┌──────────┐ ┌──────────────┐  │
│  │ Agent    │ │ Gateway  │ │ Desktop      │  │
│  │ (Python) │ │ (Python) │ │ (Electron)   │  │
│  └──────────┘ └──────────┘ └──────────────┘  │
└──────────────────────────────────────────────┘
```

### 3.2 后端选型推荐

```
┌──────────────────────────────────────────────┐
│           后端技术栈决策                       │
├──────────────────────────────────────────────┤
│                                              │
│  ✅ Tauri 2          ─── 继承 Terax (骨架)   │
│  ✅ Rust + Tokio     ─── 继承 Terax           │
│  ✅ Serde            ─── 继承 Terax + Grok    │
│  ✅ portable-pty     ─── 继承 Terax + Grok    │
│                                              │
│  ⚠️ 需要新增:                                │
│  ├── rusqlite (bundled) ─── FTS5 + 向量搜索   │
│  ├── git2 (vendored)   ─── Grok 风格 Git      │
│  ├── nono              ─── 沙箱 (Linux/macOS) │
│  ├── ratatui (可选)    ─── 备选 TUI 模式      │
│  └── tonic + prost    ─── 未来 ACP 需要       │
│                                              │
│  ❌ 不采用 Python     ─── 重写为 Rust         │
│  ❌ 不采用 Bun/Node   ─── 已有 Tauri 进程     │
│  ❌ 不采用 Hono       ─── Tauri IPC 已足够    │
└──────────────────────────────────────────────┘
```

---

## 四、桌面端构建环境对比

| 维度 | Terax AI | Grok Build | OpenCode | Hermes Agent |
|------|:--------:|:----------:|:--------:|:-----------:|
| **桌面框架** | **Tauri 2** | — | **Electron (BETA)** | **Electron 40** |
| **构建工具** | Vite + Cargo | Cargo | Bun | Vite + electron-builder |
| **Rust 工具链** | rustc + Cargo | **1.82.0** | — | — |
| **Node 需求** | **20+** | — | Bun 1.3 | **20+ / 22** |
| **额外依赖** | Tauri CLI | protoc + DotSlash | — | — |
| **macOS 构建** | ✅ | ✅ | ✅ | ✅ |
| **Linux 构建** | ✅ (deb/rpm/AppImage) | ✅ | ✅ (deb/rpm/AppImage) | ✅ (deb/rpm/AppImage) |
| **Windows 构建** | ✅ (exe, 已签名) | ⚠️ 尽力 | ✅ (exe) | ✅ (exe, nsis/msi) |
| **自动更新** | ✅ | — | — | ✅ |
| **包体积** | ~7-8 MB | ~80 MB | — | — |
| **签名证书** | ✅ SignPath.io | — | — | — |
| **Nix 支持** | ✅ flake | — | — | ✅ flake-parts |

### 4.1 桌面框架对比

```
Tauri 2 (Terax):
┌──────────────────────────────────────────────────┐
│  WebView (系统原生)    ← 极小 ≈5MB              │
│  Rust 后端 (原生二进制) ← 极致性能                │
│  IPC (Channel/Invoke)  ← 类型安全                │
│  安全: 无 Node.js 暴露                           │
│  跨平台: Win/Mac/Linux + 移动端(BETA)            │
└──────────────────────────────────────────────────┘

Electron (Hermes/OpenCode):
┌──────────────────────────────────────────────────┐
│  Chromium (捆绑)         ← ≈150MB+              │
│  Node.js (捆绑)          ← 安全攻击面大           │
│  IPC (ipcMain/ipcRenderer)                       │
│  性能: 内存占用高                                 │
│  跨平台: Win/Mac/Linux                           │
└──────────────────────────────────────────────────┘
```

### 4.2 桌面端选型推荐

```
┌──────────────────────────────────────────────┐
│        桌面端构建环境决策                      │
├──────────────────────────────────────────────┤
│                                              │
│  ✅ Tauri 2 (核心框架)                        │
│     理由: 7-8MB vs 150MB+, Rust 安全, 原生    │
│            唯一有真正桌面窗口的方案            │
│                                              │
│  ✅ Rust 工具链 (rustc + Cargo)               │
│  ✅ Node 20+ + pnpm                          │
│  ✅ Tauri CLI (cargo tauri dev/build)         │
│  ✅ SignPath.io (Windows 签名)                 │
│  ✅ Nix flake (可选)                          │
│                                              │
│  ⚠️ 需处理:                                  │
│   └── GitHub Actions CI 配置 (Win/Mac/Linux)   │
│   └── 自动更新管道 (Tauri updater 已就绪)       │
│   └── 企业 MSI/AppImage 构建                    │
│                                              │
│  ❌ 不采用 Electron ─── 太臃肿                     │
│  ❌ 不采用 Bun ─── 与 pnpm 冲突                  │
└──────────────────────────────────────────────┘
```

---

## 五、四项目关键依赖清单（按功能域分类）

### 5.1 AI 与 LLM

| 依赖 | Terax | Grok | OpenCode | Hermes | 融合方案 |
|------|:-----:|:----:|:--------:|:------:|---------|
| Vercel AI SDK v6 | ✅ | — | ✅ | — | ✅ **保留** |
| @ai-sdk/openai | ✅ | — | ✅ | — | ✅ |
| @ai-sdk/anthropic | ✅ | — | ✅ | — | ✅ |
| @ai-sdk/google | ✅ | — | ✅ | — | ✅ |
| @ai-sdk/openai-compatible | ✅ | — | — | — | ✅ **保留** |
| async-openai (Rust) | — | ✅ | — | — | ⚠️ 仅 ACP 使用 |
| openai (Python) | — | — | — | ✅ | ❌ 不采用 |
| anthropic (Python) | — | — | — | ✅ | ❌ 不采用 |

### 5.2 终端与 PTY

| 依赖 | Terax | Grok | OpenCode | Hermes | 融合方案 |
|------|:-----:|:----:|:--------:|:------:|---------|
| **xterm.js** + WebGL | ✅ | — | — | ✅ | ✅ **保留** |
| **portable-pty** | ✅ | ✅ | — | — | ✅ **保留** |
| alacritty_terminal | — | ✅ | — | — | ❌ 太重量级 |
| node-pty | — | — | ✅ | — | ❌ 保留 portable-pty |

### 5.3 LSP

| 依赖 | Terax | Grok | OpenCode | Hermes |
|------|:-----:|:----:|:--------:|:------:|
| 自制 JSON-RPC | ✅ | — | — | — |
| **async-lsp** (Rust) | — | ✅ | — | — |
| **vscode-jsonrpc** | — | — | ✅ | — |

**融合**: 保留 Terax 的 Rust LSP 层 + 移植 OpenCode 的自动安装逻辑 (TS)

### 5.4 数据库

| 依赖 | Terax | Grok | OpenCode | Hermes | 融合 |
|------|:-----:|:----:|:--------:|:------:|:----:|
| **rusqlite (bundled)** | — | ✅ | — | — | ✅ **新增** |
| **sqlite-vec** | — | ✅ | — | — | ✅ **新增** |
| Drizzle ORM | — | — | ✅ | — | ❌ TS 方案 |
| Effect SQL | — | — | ✅ | — | ❌ 太重型 |

### 5.5 代码质量

| 工具 | Terax | Grok | OpenCode | Hermes | 融合 |
|------|:-----:|:----:|:--------:|:------:|:----:|
| **Biome** (lint+format) | ✅ | — | — | — | ✅ |
| ESLint | — | — | ✅ | ✅ | ❌ Biome 已够 |
| Oxlint | — | — | ✅ | — | ❌ |
| ruff | — | — | — | ✅ | ❌ Python 不用 |
| clippy | — | ✅ | — | — | ✅ **保留** (Rust) |
| rustfmt | — | ✅ | — | — | ✅ **保留** (Rust) |
| **Vitest** | ✅ | — | — | ✅ | ✅ **保留** |
| cargo test | — | ✅ | — | — | ✅ **保留** (Rust) |

---

## 六、完整推荐技术栈一览

### 6.1 开发环境

```
┌──────────────────────────────────────────────┐
│              开发环境                          │
├──────────────────────────────────────────────┤
│                                              │
│  OS: macOS / Linux / Windows 10+             │
│  Rust: nightly (Tauri 2 要求)                │
│  Node: >=20.0.0 (推荐 22 LTS)                │
│  pnpm: >=9.0                                 │
│  IDE: VS Code + Rust Analyzer + Biome        │
│  Git: >=2.40                                  │
│  可选: Nix (flake), Docker, WSL2              │
└──────────────────────────────────────────────┘
```

### 6.2 前端栈 (TypeScript/React)

| 层级 | 技术选型 | 来源 | 原因 |
|------|---------|:----:|------|
| **框架** | **React 19** | Terax | 最大生态 + 桌面/Web 统一 |
| **构建** | **Tauri 2 + Vite 8** | Terax | 原生桌面 + 极速 HMR |
| **样式** | **Tailwind CSS v4** | Terax | 零运行时 + 设计系统 |
| **组件** | **shadcn/ui + Radix** | Terax | 可复制 + 无障碍 |
| **状态** | **Zustand 5** | Terax | 极简 + TypeScript 友好 |
| **终端** | **xterm.js 6 + WebGL** | Terax | 硬件加速 + 多 addon |
| **编辑器** | **CodeMirror 6** | Terax | 多语言 + Vim + 可扩展 |
| **AI** | **Vercel AI SDK v6** | Terax+OpenCode | 统一流/非流式 |
| **图标** | **HugeIcons + lucide** | Terax | 完整覆盖 |
| **动画** | **motion (framer-motion)** | Hermes | 需要时使用 |
| **图表** | **mermaid + d3-force** | Hermes+Grok | 学习图谱可视化 |

### 6.3 后端栈 (Rust)

| 层级 | 技术选型 | 来源 | 原因 |
|------|---------|:----:|------|
| **桌面** | **Tauri 2** | Terax | 原生窗口 + WebView |
| **运行时** | **Tokio** | Terax+Grok | Rust 标准异步 |
| **序列化** | **Serde** | Terax+Grok | 标准 |
| **终端** | **portable-pty 0.9** | Terax+Grok | 跨平台 PTY |
| **SQLite** | **rusqlite (bundled)** | Grok+Hermes | FTS5 + 向量搜索 |
| **向量** | **sqlite-vec** | Grok | 无需外部向量库 |
| **LSP** | 自制 JSON-RPC | Terax | 已在用 |
| **沙箱** | **nono 0.53** | Grok | Linux/macOS 内核级 |
| **MCP** | **rmcp 2.1** | Grok | 标准化协议 |
| **HTTP** | **reqwest 0.12** | Grok | 已成熟 |
| **Git** | **git2 (vendored)** | Grok | 比 CLI 快 |
| **追踪** | **fastrace + tracing** | Grok | 性能分析 |
| **内存** | **tikv-jemallocator** | Grok | 桌面端优化 |
| **图片** | **image 0.25** | Grok | 编解码 |
| **语法高亮** | **syntect** | Grok | 离线高亮 |
| **测试** | **cargo test + insta** | Grok | 快照测试 |

### 6.4 不采用的方案及原因

| 方案 | 项目来源 | 不采用原因 |
|------|---------|-----------|
| **Python** | Hermes | 性能瓶颈，Tauri 无法集成 Python |
| **Electron** | Hermes+OpenCode | 150MB+ 膨胀，Tauri 7MB 完胜 |
| **Effect-ts** | OpenCode | 学习曲线陡峭，影响招聘 |
| **SolidJS** | OpenCode | 生态小，React 足够 |
| **Hono** | OpenCode | Tauri IPC 已满足通信需要 |
| **Bun** | OpenCode | pnpm 已稳定，生态更成熟 |
| **Oxlint** | OpenCode | Biome 执行速度已满足 |
| **Drizzle ORM** | OpenCode | Rust 层无需 ORM |
| **Turborepo** | OpenCode | 单包架构更简单 |
| **monorepo (30+包)** | OpenCode | 维护成本远超收益 |
| **alacritty_terminal** | Grok | 太重量级，portable-pty 已够 |
| **ratatui** | Grok | 与 Tauri 桌面架构冲突 |
| **protobuf/tonic** | Grok | 当前不需要 gRPC (ACP 可选) |
| **Nix (强制)** | Terax+Hermes | 学习成本高，作为可选 |

---

## 七、关键风险与应对

| 风险 | 说明 | 应对 |
|------|------|------|
| **Hermes Python→Rust 重写** | FTS5 + 学习循环 + 技能系统 + 网关 → Rust | 分步重写: FTS5→Rust(核心), 学习循环→TS, 网关→独立可选 |
| **MCP rmcp 版本冲突** | Grok 用 reqwest 0.13，Terax 用 0.12 | 独立 crate 隔离 (Grok 模式) |
| **Tauri 2 + ratatui 冲突** | Tauri 是桌面窗口，ratatui 是终端 TUI | 两个模式: 默认桌面 + 可选 TUI 模式 (hermes tui 命令) |
| **依赖膨胀** | 从 200+ (Terax) 到 500+ (合并) | 惰性加载 + 特性门控 |
| **Windows 内核沙箱** | nono 不支持 Windows | Windows 用 Docker 沙箱回退 |

---

## 八、最终技术栈决策卡

```yaml
project: terax-super
base: terax-ai (crynta/terax-ai)

language:
  primary: Rust (edition 2024)
  secondary: TypeScript ^6.0.3
  python: false  # 全部 Rust/TS 实现

frontend:
  framework: React 19
  build: Vite 8 + Tauri 2
  styling: Tailwind CSS v4 + shadcn/ui + Radix
  state: Zustand 5
  terminal: xterm.js 6 + WebGL addon
  editor: CodeMirror 6
  ai_sdk: Vercel AI SDK v6
  lint_format: Biome
  test: Vitest

backend:
  framework: Tauri 2 (Rust)
  runtime: Tokio
  serialization: Serde
  database: rusqlite (bundled, FTS5) + sqlite-vec
  pty: portable-pty 0.9
  lsp: JSON-RPC (自研, Rust)
  git: git2 (vendored)
  sandbox: nono 0.53 (Linux/macOS) + Docker (Windows)
  mcp: rmcp 2.1 (隔离 reqwest)
  tracing: fastrace + tracing + OpenTelemetry
  memory: jemalloc (桌面端)
  test: cargo test + insta + criterion
  lint: clippy + rustfmt

desktop:
  framework: Tauri 2
  cross_platform: macOS | Linux | Windows 10+
  auto_update: ✅ (Tauri updater)
  signing: SignPath.io (Windows)
  package_size_target: <25MB (Phase 1)

ci_cd:
  platform: GitHub Actions
  targets: x86_64/aarch64 macOS/Linux/Windows
  extra: Docker (Linux), Nix (可选)

package_manager:
  rust: Cargo
  typescript: pnpm
```

这份技术栈决策确保:
1. **继承 Terax 的成熟栈** — 降低重写风险
2. **复用 Grok 的 Rust 基础设施** — 沙箱/FTS5/协议直接 crates.io 引入
3. **Hermes 核心逻辑 Rust 重写** — 保持性能 + Tauri 兼容
4. **OpenCode TS 逻辑直接移植** — 同语言低成本
5. **生态一致性** — 全部 pacakage manager 对齐 pnpm + Cargo

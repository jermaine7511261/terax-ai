# 更新日志

Yamet 的所有重要变更都记录于此。版本遵循项目规则：**功能性构建递增版本**（默认递增补丁号），同步四个文件（`package.json` / `tauri.conf.json` / `Cargo.toml` / `Cargo.lock`）；bug 修复不递增。用 `pnpm version-bump <x.y.z>` 递增。

## [未发布]

### 修复
- **子 agent 任务失败/超时 + 同一文件并发编辑丢失**。**并发编辑丢失**（last-writer-wins 数据丢失）：`fs_write_file` 加 `expected_mtime` 乐观并发校验（`check_mtime_cas` 纯函数——磁盘 mtime 与读取时不一致即拒绝覆盖，返回"concurrent modification"冲突而非静默覆盖），前端 `edit`/`multi_edit`/`apply_patch` 写前携带读取时的 mtime，冲突时提示重读重试；`ReadResult` TS 类型补 `mtime`。**子 agent 总失败/超时**：`runSubagent` 的 `generateText`（主循环 + 收尾 nudge）显式加 `maxRetries:1` + `timeout:{totalMs:5m, stepMs:90s}`（慢模型/瞬时错误有界重试、挂起不无限等）；`delegateMany` 每 worker 加 7 分钟整体超时（`Promise.race`），卡死子 agent 不阻塞整个 wave，返回 `worker timed out`。验证：vitest 2101（+2）+ tsc 0 + lint 0 + cargo 620（+3 CAS）+ clippy 0 + drift 通过 + build/size 预算内。

### 新增
- **调研待办续跑（0.1.26）：S2 guardrail 协议链 + S7 FastContext 预算**。**S2**（PraisonAI `guardrails/protocols.py` + `chain.py`）：`ai/guardrails.rs` 建 `GuardrailChain`（`GuardHook` Input/ToolCall/Output 三钩子 + `GuardrailResult{success,result,error}` + 首个失败短路 + 异常 fail-closed + 改写参数透传）；`default_consolidated` 收拢分散守卫——`ShellCommandGuard`（空命令/控制字符拒绝，CR/LF 防换行走私）+ `SensitivePathGuard`（.env/.ssh/凭据 basename 拒绝名单）；11 测试。**S7**（`context/fast/fast_context.py` + `context_injector.py`）：`ai/lib/fastContext.ts` 纯函数 `applyFastContextBudget`（maxFiles=10/maxLinesPerFile=100/maxTokens=4000 裁剪 + token 估算）、`prioritizePrecision`（查询词命中优先 + 小范围优先）、`fastContextPrompt`（预算声明注入 deep_search researcher 提示）；8 测试。验证：vitest 2099（+8）+ tsc 0 + lint 0 + cargo 617（+8）+ clippy 0 + drift 通过 + build/size 预算内。
- **调研待办续跑（0.1.26）：S3 goal judge + S6 skills 能力声明**。**S3**（PraisonAI goal/judge fail-open + tail-only）：`graph/engine.rs` 加 `judge_input`（只取 goal + 最近输出尾部，省 token 缓存友好）与 `decide_judge`（judge 异常 fail-open 放行不阻塞进展，连续 3 次解析失败暂停；判定矩阵 5 测试）。**S6**（skills 能力声明 + 预算 + fallback）：Rust `skills.rs` + 前端 `skills.ts` 加 `requiresTools`/`requiresEnv`/`fallbackForTools` 字段（解析 + 序列化），`skill_state`（ACTIVE/DEGRADED/UNAVAILABLE——fallback 工具缺失即不可用、需求缺失降级）+ `cap_skill_body`（4096 预算）；`convertSkillMd` 同步读 S6 frontmatter。**核实并勾销**：IM 微信/QQ 媒体下载已实现（weixin/qq `extract_media` + `download_media_items`，dingtalk/wecom/official_account 同步）——盘点文档该项为过时判断；S2 guardrail 协议链评估为「现有分散守卫已全覆盖，收拢成链为重构非缺陷」，标注按需排期。验证：vitest 2091（+5）+ tsc 0 + lint 0 + cargo 609（+12）+ clippy 0 + drift 通过 + build/size 预算内。
- **调研文档全量实现盘点落实（0.1.26，`docs/yamet-调研文档全量实现盘点-2026-08-10.md`）**：按盘点 §四 有效建议逐项落实——**S4 Todo 依赖**（`lib/todos.ts` 加 `dependencies` 字段 + `getReadyItems` 纯函数 + `validateTodos` 自引用/悬空校验，`todo_write` schema 支持依赖；32 测试）、**S1 doom-loop 恢复分级**（`lib/loop.ts` `recoveryNudge` 换路径→换方法→询问三级升级，`agent.ts` 连续检测计数 + `onDoomLoop` 携带 severity/message/detections，`chatStore` agentMeta 加 `doomRecovery`；16 测试）、**S5 AutoMemory 正则提取 + rules glob 激活**（`lib/autoSettle.ts` `extractReusableMemory` 提取 preference/decision/gotcha 并优先沉淀，`lib/projectRules.ts` 规则文件发现 + scope glob 匹配 + 排序；25 测试）、**agentskills 转换器**（`lib/skills.ts` `convertSkillMd` SKILL.md frontmatter+body → skill.json，含 toolAllowlist/handle 映射；29 测试）、**调研待办池**（ROADMAP 新增「调研采纳待办」节，链接来源文档 + 勾选状态）。验证：vitest 2086（213 文件，+27）+ tsc 0 + lint 0 + cargo test 597 + clippy 0 + drift 通过 + build/size 预算内。
- **第二十五轮（0.1.26）AI 子系统全原生内置**（P0-P4，`src-tauri/src/modules/ai/`）。用户命题「harness、记忆、skill、Prompt、Context、Loop、多智能体、Graph、computer use 全部原生内置」：AI 运行时核心从前端 TS 下沉 Rust，前端只留展示与薄交互壳。**P0 基建**——`client.rs`（OpenAI-compatible 流式客户端，走 `net::safe_client_for_url` 同一 SSRF 守卫；SSE 解码 / tool-call 分片累积纯函数）、`context/token.rs`（bytes/4 + 图片 2000）、`harness/`（`AiSessionState` 会话状态机 + `Channel<AiEvent>` 事件流 + `ai_session_open/close/abort/status/send` + `loop.rs` 判定矩阵）；**P1 引擎**——`context/compact.rs`（复现 `compact.test.ts` 契约：头尾保护 / 永不切 toolResult / debouncer / prune）、`prompt.rs`（`wrap_untrusted` + defang 零宽空格转义 + `neutralize_reminder_tags` + `scrub_memory_echo` + 分节模板）、`skills.rs`（skill.json 解析 / handle 校验 / 路径安全）、`ai_estimate_tokens`/`ai_estimate_messages`；**P1.5 网络**——`net/web_search/`（免 key DDG 下沉 Rust：`uddg` 还原、anomaly/captcha/mojibake 假成功检测、词汇级重排 junk 永不降级泄漏 + registrable-domain 去重、类型化失败分类 + 429 Retry-After、`SearchProvider` trait 预留 key 化源）+ `net/web_fetch/url_safety.rs`（**secret-in-URL 四路整体拒绝**：raw/unquote/normalized/unquote-normalized，`SecretInUrl`）；**P2 状态**——`memory/`（三层作用域 global/workspace/session 持久化 `ai-memory.json`，召回 = 词汇打分 + CJK 2-gram + 时间衰减 + MMR + min_score，命令 `memory_remember/recall/stats`）、`graph/`（`engine.rs` 拓扑波浪 + `hash_graph_def` journal 键 + `approval.rs` 三态级联）、`agents/`（capability_mode 交集子不越权 + 深度上限剥 task + `output_schema` 校验 + summary 预算封顶）；**P3 权限**——`research/`（L3 deep_search：plan→poll 会话状态机 + `#dr` 预算 reserve/refund + exact-ID 完整性校验 + 报告合成，命令 `deep_search_start/poll/abort/advance/reserve`）、`modules/computer/`（Windows M1 截屏 BitBlt→PNG data-URI + 缩放预算，M2 输入注入 needsApproval + 敏感区黑名单 + 动作预算 50/会话 + 视觉回环 fail-closed，`computer_session_open/close`/`computer_approve/revoke`/`computer_capture/action`）；**P4 收尾**——`useNativeAi` 偏好开关（settings→Agents，双轨切换默认关闭 + 持久化测试锁定）、`web_search` 前端薄壳化（删冗余 JS DDG 解析）、`scripts/bench-native.mjs` 基准。**补修**：MUST 差距 1——deep_search researcher/verifier 工具面补 `web_search`/`fetch_url`（`general` 子 agent 白名单 + `runSubagent` 读池接 `buildNetTools`，防研究阶段实际离线）+ L1 抓取清洗增强（`net/web_fetch/clean.rs`：两级 retry fallback + 5000 字符噪声反转护栏 + 链接密度惩罚 + OG/Twitter/title 三级元数据）+ **子代理空返回修复（子代理通道故障）**——根因：`generateText` 的 `result.text` 只取 `finalStep.text`，子代理多步工具调用后若停在工具调用步（cap 命中），`finalStep.text` 为空导致 summary `"(no output)"`。修复：① `registry.ts` 新增 `CLOSING_RULE` 追加到每个子代理 system prompt（最终消息必须是纯文本总结，禁止工具调用）；② `runSubagent.ts` 兜底——工具循环结束仍无文本时，移除工具再跑一轮「仅输出总结」nudge，保证子代理必产 summary；③ 测试锁定（nudge 重试 + closing 规则 + 正常文本路径）。全部子代理调用方（deepSearch/delegateMany/graph）继承修复。验证：vitest 2059 + tsc 0 + lint 0+ **CLI/WebUI 补齐**——`cli.rs` `yamet --prompt` print-mode 单次对话出口（复用 `ai::client` + keyring，stdout 流式）+ `dev:web` 脚本（vite :1420 + Node 后端 WS :31219）+ web 服务端域扩展（git 只读 status/log/diff/branches + history）+ web 引导安全（window.show/pty_close_all 降级）+ `smoke.test.ts` 锁定命令面。**Web 服务端安全加固（源码级四视角调研，MUST）**——WS 绑定 127.0.0.1 + Origin 白名单 + 每帧 token 鉴权（`dev-web.mjs` 注入 `VITE_WS_TOKEN`）；fs 路径包含性校验 + 敏感文件门 + 大小上限；`shell_run_command` cwd 约束 + 进程树杀；写面运行时限（`WRITE_BLOCKED` 拒 git 写/shell_bg/pty）；CSP `frame-src` 收紧（去掉裸 http:/https:）+ `assetProtocol` scope 收窄（`$HOME`/`$APPDATA`/`$RESOURCE`/`$TEMP`）；`check-doc-drift` 新增 web handler 命令契约检查；`git_log` limit 钳位。验证：cargo test 597 + vitest 2057（212 文件）+ tsc 0 + lint 0 + drift 通过 + build ✓。**安全**：密钥 keyring 内解析（`secrets::read_key`）前端只传 keyring account、模型调用全程走 SSRF 守卫、web 结果 untrusted 标注。验证：cargo test 597（lib）+ clippy -D warnings 0 + vitest 2059 + drift 通过 + lint 0（tsc 预存 39 错误来自先前未提交工作，非本轮）。**版本 0.1.25 → 0.1.26**（四文件同步）。
- **第二十四轮（0.1.25）Office 套件全功能内置**（深度移植，全原生 Rust）：核心引擎选用 **office_oxide**（纯 Rust、本就在依赖树中，零新增编译成本）+ pdf_oxide + lopdf。① **六格式读取**——`fs_read_file`（AI 读路径 `source=="ai"`）按 magic/扩展名解析：OOXML（DOCX/XLSX/PPTX）+ **旧二进制 DOC/XLS/PPT**（OLE2/CFB 魔数 `D0 CF 11 E0` 嗅探 + 扩展名，office_oxide `plain_text`）+ PDF（pdf_oxide）；编辑器/资源管理器路径仍视为 binary（PDF iframe 预览不回归、保存不会用文本覆盖二进制文档）；② **四格式创建（富格式）**——`create_docx`（markdown 风格行 → office_oxide 引擎，正确 styles/numbering 的 Word：标题/粗斜体/项目符号/管道表格）、`create_xlsx`（单元格自动识别数字/公式 `=` 前缀 → 数值单元格 + 公式 + 粗体表头）、`create_pptx`（每字符串一页：首行标题、`- ` 项目符号、其余正文 → PptxWriter 富文本）、`create_pdf`（新增，lopdf 文本渲染 A4：`# ` 标题字号阶梯、自动换行分页、Helvetica 基础字体）；③ **三格式编辑（就地保真写回）**——`edit_docx`/`edit_pptx`（`EditableDocument::replace_text`，未改动 part 逐字节保留）、`edit_xlsx`（`set_cell` 保留原单元格样式），均返回替换/设置次数；④ **PDF 处理**——`merge_pdf`（按序合并，lopdf 0.44 无 `Document::merge`，按官方 merge 示例手写重编号 + 重建 Pages/Catalog + 重设 Parent）、`encrypt_pdf`（AES-256 V5）、`fs_pdf_page_count`；⑤ **安全/稳健**——全部写命令经 `checkWritableCanonical` 拒绝名单 + Rust `enforce_ai_workspace_authorization` 双门禁 + 原子写；编辑命令额外读门禁 + ≤50MB 上限；读取 zip-bomb/输出上限（单 XML 条目 ≤64MB、总输出 ≤8MB）；⑥ **PDF 创建踩坑修复**——资源必须包在 `/Font` 字典（否则提取为空）、`Td` 是相对移动需每行独立 `BT…ET` 绝对定位（`Td` 在单文本对象内为相对位移）；⑦ **参考项目**——E:\Agent 下载 `office_oxide-main`/`betteroffice-main`/`ppt-rs-main`/`markdown2pdf-main`/`opendocswork-mcp-main`/`openxml-office-release` 供后续参考（betteroffice xlsx-ops 在 xlsx 深层编辑更强但 yrs CRDT + 185 包较重、PPTX 保存未投影编辑，本轮选用 office_oxide）。验证：cargo test 431（lib，新增 11）+ clippy -D warnings 0 + vitest 1922（新增 12）+ tsc 0 + lint 0 警告 + drift 通过 + `pnpm build`/`pnpm size`（eager 359/540KB、total 1.56/1.6MB）。**版本 0.1.24 → 0.1.25**（四文件同步）。
- **第二十三轮（0.1.24）基础功能打磨增强**：① **P0-1 技能合并竞态**——useAiBootstrap 的 snippets hydrate() 与 scanSkillsDir→mergeBuiltin 并发执行，hydrate 完成时 builtin 会被其 set({snippets}) 覆盖丢失；改为 hydrate 完成后串行合并，补顺序断言测试；② **P0-2 MCP cwd 绑定错误**——McpServerConfig 新增可选 cwd（配置时固定启动目录），mcp_server_connect 优先用配置 cwd（resolve_connect_cwd 纯函数 + 3 测试），侧栏 McpSidebarPanel 不再把当前工作区 root 当服务器启动目录；③ **P0-3 后台进程条目永不回收**——shell_bg_* 的 bg map 只增不删（每条持 4MiB 环形缓冲）；抽 prune_bg_map 纯函数（spawn/list 清理 exited + 64 条 LRU 逐出最旧），kill 后同步 remove（+2 unix 测试）；④ **P0-4 pty-helper 重连 id 竞态**——client.rs 重连种子固定 sleep(200ms) 读 SessionList，大负载下 id 低估可撞现存会话被杀；改 Condvar 等首帧 SessionList（1s 超时兜底）再派生 next_id；⑤ **P1-1 fs_rename Unix 覆盖竞态**——预检 exists() 后 rename(2) 有 TOCTOU 窗口，Linux 改 renameat2(RENAME_NOREPLACE)、macOS 改 renamex_np(RENAME_EXCL)，check+move 原子化；⑥ **P1-2 sftp_read OOM**——下载后 read_to_string 整体入内存，抽 read_bounded_text（metadata 预检 + take(cap+1) 分块读）+ 2 测试；⑦ **P1-3 shell 并发上限**——ShellState 加 Arc<Semaphore>（8 并发），shell_run_command/shell_session_run 等待获取、shell_bg_spawn 快速失败；⑧ **P1-4 ssh identity_file 空格被拒**——clean_component 拒空白使 C:\Users\John Doe\.ssh\id_ed25519 无法连接，新增 clean_identity_path（保留空格，仅拒空/控制符/前导 -）+ 2 测试；⑨ **P2-1 死命令面**——移除 pty_helper_list/pty_helper_start 的 IPC 注册；⑩ **P2-2 fs_write_file 无上限**——MAX_WRITE_BYTES 64MiB 写前拒绝；⑪ **P2-3 App 渲染期写 ref**——tabsRef.current = tabs 移入 useEffect；⑫ **P2-4 Onboarding 能力 CTA**——aiKey/capabilities 卡片可点击直达设置「模型」/「集成」tab。验证：tsc 0 + vitest 1898（197 文件）+ cargo test 全绿（lib 395 + 集成）+ clippy -D warnings 0 警告。**版本 0.1.23 → 0.1.24**（四文件同步）。
- **第二十二轮（0.1.23）Harness / 工具 / 底座打磨**：① **P0-1 platform/web 补测**——`path.test.ts`（join 各 case：空/绝对/分隔符/`.` 段）、`os.test.ts`（jsdom mock navigator：win/mac/linux + arch）、`events.test.ts`（listen/emit/unlisten + 多监听器 + 事件隔离），platform 层测试从 1 个升到 4 个文件 19 测试；② **P0-2 mcp 抽纯函数**——`mcp.ts` 的 `sanitizeToolName`/`formatMcpResult` 抽到 leaf 模块 `lib/mcpFormat.ts`（字符清洗 + content 格式化，13 测试）；③ **P0-3 graph 抽纯函数**——`graph.ts` 的 graph JSON 解析 + `g-<name>-<ts>` id 派生抽到 `lib/graphParse.ts`（`parseGraphDef` 校验 nodes/edges + `deriveGraphId` 确定性 id，9 测试），graph.ts 改用之；④ **P1-0 注册面完整性**——`tools.test.ts` 锁死 buildTools 挂载面（30 个工具 key 全含 + 无重复，防漏注册）；⑤ **P1-1 coverage 阈值提升**——33/29/27/33 → **35/30/28/35**（补测后实测 35/30.62/28.59/36.04，全超阈值）。验证：tsc 0 + vitest 1891（196 文件，新增 43 测试）+ coverage 阈值过 + cargo test 全绿。**版本 0.1.22 → 0.1.23**（四文件同步）。
- **第二十一轮（0.1.22）Grok 网络工具整体移植**（全功能移植 grok-build 源码，三层 L1→L2→L3）：① **L1 `web_fetch`**——`src-tauri/src/modules/net/web_fetch/` 移植 Grok `web_fetch/` 8 文件（`ssrf.rs` IPv4/IPv6 完整特殊地址分类 + DNS rebinding 双门控 + `allow_local` 仅显式 loopback、`domain.rs` 域名白名单 DomainMatcher（约 60 个开发文档域，host+path 前缀 O(1) 匹配）、`config.rs` WebFetchParams（TTL 900s/缓存 128/超时 60s/10MB 上限/100KB markdown）、`http.rs` 连接池原子失效防中毒、`cache.rs` TTL 缓存、`client.rs` HTTPS 升级 + 同主机重定向逐跳复检 SSRF + HTML→markdown（htmd+scraper）+ base64 剥离 + 截断、`types.rs` WebFetchError 16 种 + Grok 的"SSRF 拦 github 提示改用 gh CLI"智能提示），暴露 `web_fetch` Tauri 命令（Cargo 加 reqwest gzip/brotli/deflate + htmd 0.5.4 + scraper + parking_lot）；② **L2 `web_search`**——免 key 的 DuckDuckGo HTML 兜底实现（`native.httpGetText` 走 Rust SSRF 代理 + `parseDdgResults` 正则解析，无外部依赖开箱即用）；③ **L3 `deep_search`**——移植 Grok deep_research.rhai 的 4 阶段编排（Plan 拆解 → Research 并行收集 claims → Verify 交叉验证 → Report 合成引用报告）到前端子代理栈（runSubagent + web_search/fetch_url），`deepSearch.ts` 纯编排可注入 runner 测试；④ 前端接线——`native.ts` 加 `webFetch`/`httpGetText`，`tools/net.ts` 注册 `fetch_url`/`web_search`，`tools/deepSearch.ts` 注册 `deep_search`，`registry.ts` 同步三工具描述。验证：cargo test 40（web_fetch SSRF/domain/http/cache/client）+ 真实网络抓取 docs.rs 转 markdown + SSRF 拦私网；vitest 1848（新增 fetch_url 5 + web_search 4 + deep_search 4）+ tsc 0 + `pnpm build` exit 0。**版本 0.1.21 → 0.1.22**（四文件同步）。
- **第二十轮前端接线补齐（webui/tui/cli/gui 一体共享核心）**：① AgentSwitcher 改用 `selectablePrimaryAgents` 过滤（P1-0 mode/hidden 语义落地，隐藏 subagent-only/hidden agent）；② `scrubMemoryEcho` 真正接进 `agent.ts` onFinish（P1-4 标记隔离闭环——注入侧有标记、输出侧清洗模型回显防被当输入，抽 `lib/memoryMarker.ts` leaf 模块避免 transport↔agent 循环依赖）；③ `delegate_many` 每个 fan-out 用 `createSubSession` 建真实子会话（P2-2 parentID 树落地），SessionBar 按 parentId 缩进展示会话树。验证：tsc 0 + vitest 1739 + biome lint 0 error + `pnpm build` exit 0。
- **第二十轮（0.1.21）Multi-Agent / Graph Engineering 原生内置**（全层融合，概念模型驱动）：① **P0-1 轻量 Graph 编排引擎（L4+H6）**——前端 TS 编排器（`graph/engine.ts`）拓扑波浪调度 + 并行（Semaphore）+ judge 分支剪枝 + human 审批 + merge 聚合 + worker/verify 双轮；`graph/journal.ts` 断点续跑（request_hash 去重）；`graph/store.ts` 运行态 + `GraphRunPanel.tsx` 节点可视化（已挂 AiChatPanel）+ `run_graph` 工具（needsApproval）；② **P0-2 并发 Worker 委派（L3）**——`delegate_many` 工具（并行上限4、深度上限 MAX_SPAWN_DEPTH=3、每 worker 独立上下文、summary 封顶 4000、per-fanout 预算 consume/refund），`agentActivityStore` 加 group/depth/parentId；③ **P2-2 子会话 parentID 树（H2）**——`SessionMeta.parentId` + `createSubSession` + `resolveRootSessionId`（cycle-safe）；④ **P2-3 IterationBudget（H5）**——`lib/budget.ts` consume/refund + 模块级活跃预算；⑤ **P1-1 Loop 状态机（L2）**——`lib/loop.ts` phase（thinking/calling/observing/done）+ 健壮退出 + doom-loop 检测，接入 agent 循环并展示于 ActivityStrip；⑥ **P1-0 Agent 配置 schema 化**——`agents.ts` mode/hidden + `mergeAgentOverrides`（opencode 同名覆盖/disable 删除）+ 选择过滤；⑦ **P1-4 记忆系统重构（H7）**——召回式注入（`recallScore/recallTop` 按最新用户消息召回 top-8，替代全量拼接）+ `[System note: recalled memory context]` 标记隔离 + `scrubMemoryEcho` 清洗 + `onTurnComplete` 自动沉淀（`autoSettle.ts` source:"auto"）；⑧ **P1-5 skill 自动策展（curator 式）**——`skillCurator.ts` 生命周期状态机（archive/pin/keep，只动 agent 创建、pinned 豁免、永不删除）+ 后台 `runBackgroundCurator` 每小时触发；⑨ **P1-3 human 审批三态**——`lib/approval.ts` once/always/reject + 级联 auto-approve + reject 反馈，GraphRunPanel 升级三态按钮；⑩ **P2-1 上下文压缩四元接口**——`compact.ts` shouldCompress/selectContext（头尾保护区 PROTECT_FIRST/LAST_N）/createCompressionDebouncer（防抖动门）/pruneToolResultsOnly。tsc 0 错误 + vitest 741（AI 模块）+ 全量 1729 + cargo test 全绿 + `pnpm build` exit 0。**版本 0.1.20 → 0.1.21**（四文件同步）。
- **第十九轮（0.1.20）** 模型/智能体状态栏增强：① 模型与智能体分离——`AgentSwitcher` 拆出独立 `ModelSwitcher`，状态栏两个并列下拉（`AiStatusBarControls`）；② 模型选择持久化——`selectedModelId` 存入 preferences，启动经 `useRestoreSelectedModel` 恢复；③ 状态栏上下文占用百分比——新增 `contextEstimate`（`approxBytesFromUI`，与压缩逻辑 `compactModelMessagesDetailed` **同源的近似 token**）÷ `getModelContextLimit`，`ModelSwitcher` 触发器显示 `NN%`，≥55% 琥珀 / ≥70% 红（对应压缩触发阈值），参考 Hermes 状态栏 context_pct；④ 思考长度选项卡——`ModelSwitcher` 下拉新增思考长度档位（off/low/medium/high，持久化 `thinkingLength`），经 `providerOptions.reasoning_effort` 传入模型请求。tsc + vitest 1646 + i18n-scan + drift + lint 全绿。**版本 0.1.19 → 0.1.20**（四文件同步：package.json / tauri.conf.json / Cargo.toml / Cargo.lock）。
- **审查修复·二次（2026-08-07，深度测评四视角）**：① Windows 进程树 kill 根治——`shell_run_command` 在 spawn 后立即创建并持有 Job（此前超时分支才创建，等待期孙进程可逃逸），超时分支直接 `terminate()`；② `ai_http_request` 并发内存护栏——`ResponseQuotaGuard` 按请求预留 64MiB 配额、全局 256MiB 预算超限拒绝（含释放回归测试）；③ scheduler 持久化改原子写（tmp+rename+sync，Windows rename 冲突回退 remove+rename，含测试）；④ `fs/policy.rs` 秘密清单正则改 `OnceLock` 预编译；⑤ `git push_upstream` 的 remote 参数复用 `is_remote_name_char` 校验（防 `--delete` 选项注入）；⑥ check-doc-drift 升级契约级检查——前端 `invoke()` 字面量命令名必须注册于 `generate_handler!`（跳过注释行），首跑即抓到真实漂移：`platform/tauri/watch.ts` 调用未注册的 `fs_watch`/`fs_unwatch`（实为 `fs_watch_add`/`fs_watch_remove`），已修复并接线 `fs:changed` 事件流；⑦ `lib.rs` 147 命令按域分组注释（PTY/SSH、fs、LSP/DAP、搜索、git、shell/workspace、agent/secrets、gateway、AI 网络、MCP/窗口/调度）；⑧ settings 层测试破零——`ModelsSection` 抽 `modelsLib.ts`（isProviderConfigured/splitProviders/createEndpointDraft/patchEndpoint/nextModelIdAfterEndpointRemoval/fullyConfiguredEndpoints，14 测试）；⑨ `SourceControlPanel` 抽 `lib/format.ts`（dirname/entryPathLabel/upstreamBadgeLabel/statusAccent/checkboxValue，9 测试），`App.tsx` 抽 `useOnboarding`/`useRestoreSelectedModel` hooks；⑩ 新增进程级 E2E 冒烟 `tests/cli_entry.rs`（真实二进制走 `__mcp_server` stdio 握手 + 干净退出，跨平台进 cargo test）；⑪ 新文档 `docs/webui-roadmap.md`（MVP 边界）、`docs/e2e-smoke.md`（tauri-driver 方案）、`docs/release-verification.md`（发布 checklist）；⑫ 清理 `scripts/debug-*.mjs` 3 个 + src-tauri 下 bogus/check/ver.log 3 个；⑬ 修 `ModelSwitcher` tsc：补 `ai.manageModels` i18n 键（zh/en）；⑭ 配套未提交的 selectedModelId 持久化改动，补 `chatStore.test.ts` 的 `setSelectedModelId` mock（2 个测试恢复绿）。验证：cargo test 全绿（lib 347 + 集成含 cli_entry）、vitest 全绿（173 文件 1646 测试）、tsc 0 错误、drift 含新契约门禁通过、vite build exit 0。
- **审查修复·发布链路 + 安全 + 卫生**：① Windows 超时 kill 进程树逃逸修复——`ProcessJob` 新增 `terminate()`（`TerminateJobObject` 杀整棵进程树），`shell_run_command` 超时 kill 路径此前 `let _ = create_for()` 立即丢弃 job 句柄致 KILL_ON_JOB_CLOSE 失效，改为持有 job + `terminate()`；`shell_bg_kill` 同步显式 terminate；新增 `terminate_kills_assigned_process_tree` 测试。② `ai_http_stream` 补读超时（此前 `req.send()`/`stream.next()` 无 timeout，服务器连接后不响应会无限挂起；`ai_http_request` 已有 60s/120s timeout，本次仅补流式路径）。③ CHANGELOG 覆盖阈值漂移修正（第12轮记录 45/35/30/45 与 `vite.config.ts` 实际 29/25/23/29 不符）。④ README 发布链路占位如实标注（updater 端点 `{owner}/{repo}` 为 GitHub Actions 发布模板，fork 后需替换 `your-org`）。⑤ 清理 16 个一次性 `scripts/fix-*.mjs` 迁移脚本（第十六轮平台迁移产物，零引用）。cargo test 401 + clippy/tsc/drift 全绿 + vitest 1623。
- **工作区 cwd 状态栏 + AI 授权修复**：① 状态栏 cwd 在无终端 leaf cwd 时回退到当前空间 root，修复"已选工作区但底部显示 no directory"（`activeCwd` 仅读终端 leaf cwd，编辑器 tab 或终端未报 cwd 时退化为 null）；② 切换空间时用该空间 root 调用 `workspace_authorize` 注册后端授权 registry，修复"E:\Agent\yamet 超出授权工作区范围"（此前仅终端 cwd 触发授权，自定义空间无终端打开时 AI 写路径不被授权）。
- **审计修复批次·五（0.1.19 后）** AI 文件安全模型收紧（P1-3，安全修复 + 注释）：写路径 `enforce_ai_workspace_authorization`（fs/mod.rs）补齐敏感清单校验 `policy::check_readable`，与读侧对称——AI 写 `.env`/私钥/受保护目录（即使在工作区内）一律拒绝（此前仅读侧拦）；`fs/mod.rs` 头注释改为如实描述"纵深防御层（AI 本有 shell 权限，source 客户端可传非加密）"；`workspace.rs is_authorized` 补前缀匹配语义注释。新增 3 项断言测试（AI 写 .env 拒绝 / AI 写普通文件放行 / 用户写不受门禁）。cargo test 401 + clippy/tsc/drift 全绿。
- **审计修复批次·四（0.1.19 后）** verify 门禁清偿·lint 门禁转绿：修复 `src/platform/index.ts` `sendNotification` 的 `noVoidTypeReturn` 错误（`: void` 函数却 `return` 返回值，改为直接调用不返回）。这是 biome lint 门禁的 2 个阻塞 error（此前 verify 的 `pnpm lint` 一直红）；现 `biome lint ./src` 0 error。tsc + vitest 1623 全绿。
- **审计修复批次·三（0.1.19 后）** 前端硬编码英文收敛 i18n（P1-1，~35 处）：AiChat / AiMiniWindow / AiComposerInput / composer.tsx / SourceControlPanel / App.tsx 全部写死英文改为 `t()`/`tStatic()`。新增 33 个 `ai` 组键（editorSelection/terminalSelection/emptyDescription/continueHint/dismiss/contextCompactedOne|Many/stepLimitHit/continue/fileCountOne|Many/listening/loadingSessions/unpin/pinOnTop/lastRequest/estimatedContext/lastRequestHint/tokenCountApprox/today/yesterday/editsQueued/noEditsQueued/exit/terminalSeesHint + 3 组 suggest 建议键）+ `spaces.newSpaceName` + `git.discardAllBody/discardFileBody`（zh/en 双语言补全，AssertSameKeys 编译期奇偶门校验）。复用 ai.thinking/planMode/requestFailed、common.cancel、git.discard、dialogs.discardChanges。tsc + vitest 1623 + i18n-scan 全绿。
- **审计修复批次·二（0.1.19 后）** 后端 P1 修复 + verify 门禁修复：`fs_copy` 接入 AI 源门（拆纯函数 `fs_copy_impl` + 薄命令壳；AI 源拷贝时目标须在工作区授权内 + 源路径跑敏感清单 `check_read_path_authorized`，封闭"任意绝对路径拷入工作区绕过秘密清单"缺口，新增门禁断言测试）；scheduler `tick()` 重构（昂贵 `next_trigger` 扫描移到锁外，坏表达式不再持写锁阻塞调度命令面）；verify.ps1 CHANGELOG 门禁修复（预存在 PowerShell 通配符 bug：`-like "[未发布]*"` 把 `[` 当通配符抛 WildcardPatternException，改 `-match "^\[未发布\]"`；补 UTF-8 BOM 防 Windows PowerShell 5.1 把中文字面量读成 ANSI 乱码）。cargo test 398 通过 + clippy/tsc/drift 全绿。
- **审计修复批次（0.1.19 后）** 深度测评 P1 修复 + 门禁清偿：修复迷你窗 React #185 无限渲染循环（`useRecentActivities` 选择器返回新数组引用，改为选稳定 `s.activities` 再切片，`ActivityStrip` 不再死循环）；MCP server `initialize` 回显客户端请求 id + 协商 protocolVersion（不再硬编码 id=1/2024-11-05），且 `notifications/initialized` 通知不再回发响应（符合 JSON-RPC 2.0）；pty-helper 认证 token 文件 Unix 写 0600 属主权限（防终端劫持）；gateway 移除微信私聊无条件 auto-approve（weixin 已过滤自身消息，抵达 registry 的必为第三方，统一走默认拒绝门）；前端两处绕过 `@/platform` 直连 `@tauri-apps/*` 收敛（`terminalClipboard.ts`、`PreviewAddressBar.tsx`）；check-doc-drift 门禁补 `sftp_write` 恢复绿。新增 agentActivityStore 引用稳定回归测试 + MCP server id/通知测试。cargo test 397 通过 + clippy 全绿 + tsc 全绿 + drift 门禁通过。
- **存储适配器 vitest 修复**：`createTauriStorage` 在非 Tauri 运行时（node/jsdom 测试、web）降级 per-file 内存 Map，不再触及需 `window` 的 plugin-store；`onChange` Promise 拒绝被捕获。消除 66 个未捕获 rejection（vitest 由 exit 1 恢复绿，1623 测试全过）。
- **第十八轮（0.1.19）** 剩余 deferred 全量实现：helper PTY 滚动缓冲（HelperSessionHandlers 加 RollingBuffer + Frame::Output 镜像 + pty_helper_buffer_lines 分页命令，闭合第十四轮 T 的 helper 路径缺口）；useSourceControlPanel（1237 行）补测 30 个 + useTerminalSession（1220 行）补测 22 个（Coverage 两个最大 P0 文件零测试缺口闭合）；工作区最近打开（pushRecent + recentSpaces 纯函数 + SpaceSwitcher 渲染最近列表）。1621 前端测试 + cargo clippy 全绿。
- **第十七轮（0.1.18）** 全量补全：远程 SFTP 编辑回写（sftp_write 后端 + RemoteEditorDialog，闭合审计远程只读缺口）；统一 EmptyState（MCP/网关侧栏引导空态）；迷你窗 P1-a 边缘吸附（snapGeom 距边16px自动吸附）+ 双击最大化/还原 + P1-b 置顶 pin（z-40→z-50）；MCP 添加失败 toast 反馈；DAP 适配器缺失错误 banner；git-history 侧栏入口；会话导出全部（Markdown 下载）+ 清空全部按钮。闭合第十五轮 P1/P2 需求、audit-user-ux/audit-frontend 产品缺陷。1557 前端测试 + cargo clippy 全绿。
- **第十六轮（0.1.17）** 平台抽象层迁移（GUI/WebUI/TUI/CLI 共享核心逻辑）：全量收敛 `@tauri-apps/*` 直接引用到 `@/platform` 统一适配器层（136 处归零，仅 `platform/tauri/` 自身保留）——核心 IPC（invoke/Channel/convertFileSrc + 新增 invokeRaw 支持 PTY raw-body 写）、事件（listen/emit/UnlistenFn）、存储（LazyStore→createStorage，autoSave 语义由 adapter.set 自动 save 保证）、窗口/路径/开盖/os/app/process/dialog/notification/autostart/updater/clipboard 便捷函数，每个未初始化回退 Tauri 原生（测试 mock 生效）。架构：`types.ts` 16 接口 + `tauri/` 16 适配器 + `web/` 实现，`detectPlatform()` 按运行时选适配器。1557 前端测试全绿 + cargo clippy 通过。
- **第十五轮（0.1.16）** AI 聊天窗口补强：主窗口会话历史+新建(SessionBar 顶部栏含+新建+下拉切换/重命名/删除); 主窗口 Todo 展示(AiChatPanel 底部 TodoStrip, 与迷你窗共享 todoStore); Edit/Write 工具完成后可折叠展开查看改动内容(旧/新编辑内容); 迷你窗子 agent 进程视图(ActivityStrip 卡片化显示 subagent 类型/实时 step/summary/耗时); 迷你窗内嵌输入框(AiComposerInput 复用顶层 composer); 会话切换草稿保持(composer 按 sessionId 分键保存/恢复)。
- **第十四轮（0.1.15）** 原生能力深化：终端滚动缓冲区下沉 Rust（新增 `pty/buffer.rs` RollingBuffer 环形 10k 行镜像 + `pty_buffer_lines(id,count,end)` 分页命令 + 前端 `PtySession.bufferLines`，大输出可经后端分页不占前端内存）；文件搜索增量缓存（新增 `fs/index_cache.rs`，按根目录子条目 name+mtime 签名缓存 `(root,query)->hits`，树未变跳过 walk+rank，缺失/变更回退全扫，正确性不依赖缓存）；会话持久化容错（`loadAll` 顶层容错，store 文件整体损坏回退空态防启动崩溃，已有 LazyStore 原子 + hydrateTabs 损坏条目跳过）。
- **第十三轮（0.1.14）** 产品成熟度补强：DAP 适配器缺失检测 + 可复制安装命令引导（多语言 launch.json 模板 node/rust/go）；五面板引导性空状态（dap/mcp/remote/search/gateway 用 EmptyState 组件）；Onboarding 能力引导 + Windows 首启签名提示；coverage 渐进基线 21%（补 6 轮测试，statements 18.76%→21.35%、测试 1020→1240）；命令面板搜索 + 快捷键搜索 + 搜索替换正则；主题扩充 + 导出/导入；工作区最近打开；会话导出/清理 + 容量提示；扩展边界固化 + i18n 扫描门禁 + 平台排障文档。
- **第十二轮（0.1.13）** 测试覆盖率接入：前端 `pnpm test:coverage`（v8 provider，CI 强制 statements / branches / functions / lines 四维阈值，渐进式逐步上调——当前 `vite.config.ts` 为 29 / 25 / 23 / 29，后续每轮按实测核心逻辑价值递增）；新增零测试模块覆盖（mcp store 状态机、dap store 会话/断点/栈帧、source-control remote indicator、gateway pending-meta 状态机）；Rust DAP 真实子进程 fake-adapter 集成测试（grok 思路，不 mock 传输层）。
- **第十二轮（0.1.13）** 漂移清偿 + 防漂移门禁：YAMET.md 修正 dap 命令面（统一为 `dap_session_create/connect/disconnect/list/get` + `dap_request_send`）、补 mcp 命令面与 mcp/search/gateway 模块布局、补启动命令；两篇英文架构文档（dap-protocol/mcp-protocol）汉化；需求汇总补第十/十一轮；ROADMAP 勾选发布自动化与打包体积优化；新建 `scripts/check-doc-drift.mjs`（命令面 / 模块布局 / 原生铁律三查）挂 verify + CI。
- **第十二轮（0.1.13）** i18n parity 恢复：en 补 54 个快捷键键；重新启用 `AssertSameKeys` 编译期类型守卫（zh/en 键集不一致即 `tsc` 报错）。
- **第十二轮（0.1.13）** 原生铁律固化：DAP/MCP/PTY/LSP + Skill/技能沉淀/插件系统必须原生（宿主/传输/UI 层 Rust 原生或 Yamet 自身实现，禁 tmux/vscode-debugadapter/js-debug/Node-Python 常驻桥接/非原生插件运行时）写进 YAMET.md 质量门槛；清理 `adapter.rs` vscode-js-debug 过时注释。
- **第十二轮（0.1.13）** 产品成熟度：硬编码英文 UI 清零（Commit Graph / No repository / No commits yet / Of which cached / Commit staged changes 等 5 处 + 3 处空状态文案走 i18n）；DAP 面板新增「创建示例配置」（一键写 `.yamet/launch.json` debugpy 示例，已存在不覆盖）；首启引导增加「配置 AI 密钥」步骤；空状态审计（dap/mcp/remote/search/gateway 五面板已达标）与静默 catch 审计（29 处均为 best-effort 分支，无需改）。
- **第十一轮（0.1.12）** 原生组件整合（以 0.1.12 为基座叠加 LSP/PTY/DAP/MCP 四大原生组件增强）：DAP 重构为完整 session+transport 模型（stdio/tcp 传输、`dap_session_create/connect/disconnect/list/get` + `dap_request_send`，前端 `modules/dap` 全新 DebugPanel + breakpointGutter 双向同步）；MCP 原生化（stdio/SSE 完整 client + server 生命周期，`mcp_server_add/remove/list/get/connect/disconnect/refresh` + `mcp_tool_call/mcp_resource_read`，前端 `modules/mcp` 全新 store + McpServersGroup）；复用 `lsp/framing.rs` 公共 Content-Length 分帧；设置页新增「集成」标签（IntegrationsSection 统一管理 DAP 适配器 + MCP 服务器）；AI 子系统 MCP 工具迁移到新原生 store（`@/modules/mcp`），删除旧 `modules/debug`/旧 `ai/lib/mcp`/旧 `ai/store/mcpStore`。
- **第十一轮（0.1.12）** SSH 健壮性修复：`SshTarget.identity_file` 接受前端 `identityFile`（camelCase）与后端 `identity_file` 两种 wire 形式（`rename`+`alias`），私钥不再被静默丢弃（原 MUST）；`sftp`/`ssh -N` 后台命令对 host/user 用 `clean_component` 校验，防 `-oProxyCommand=...` 选项注入；含空格/引号的远程路径经 batch 单引号转义；`sftp_read` 先 `ls -la` 预检 4 MiB 上限再下载（避免 OOM）；`sftp -b -` 与后台 `ssh -N` 加 `BatchMode=yes`（无 TTY 下首次连接新主机不挂死）；远程面板去掉恒真隧道 filter。
- **第十一轮（0.1.12）** DAP 调试器（Debug Adapter Protocol）：Rust 后端 modules/dap/（适配器注册表 debugpy/node-inspect/lldb-dap/gdb/dlv、复用 lsp/framing.rs 分帧、请求-响应 id 配对 + 30s 超时、reverse request 分发、孤儿响应转发现前端、fire-and-forget launch 适配 debugpy 延迟响应语义）+ 前端 modules/debug/ 调试面板（程序路径/适配器选择/启动停止/状态指示/单步/调用栈/变量树/Debug 输出），侧栏新增「调试」视图；debugpy 端到端验证通过（断点命中/调用栈/变量）。
- **第十一轮（0.1.12）** DAP 编辑器断点：CodeMirror 行内断点 gutter（红点 + 暂停高亮），编辑器断点 ↔ DAP `setBreakpoints` 双向同步；`.yamet/launch.json` / `launch.json` / `.vscode/launch.json` 配置解析与下拉选择。
- **第十一轮（0.1.12）** 远程面板（侧栏「远程」视图）：SFTP 远程文件浏览（`sftp_list`/`sftp_read`）+ 端口转发隧道管理（`-L`/`-R`，start/list/kill），接线先前仅后端实现的 SSH 后续能力。
- **第十一轮（0.1.12）** 打包体积：debug/remote 面板改为懒加载（移出 eager 启动图），xterm CSS 移入终端模块；total client JS 上限调整为 1550KB 并记录「图标集完整保留为不可减瓶颈」结论（eager 359KB 达标）。
- **第十一轮（0.1.12）** 遥测方向经维护者确认：维持现状（无遥测），与 ROADMAP/README 卖点一致。
- **第十一轮（0.1.12）** PTY Windows ConPTY 健壮性：openpty 5s 超时线程兜底（ConPTY 未初始化不再挂死）；Windows child.wait() 改 try_wait() 轮询（防无限挂起）。
- **第十一轮（0.1.12）** LSP 诊断行偏移（hermes range_shift）：diagnose.ts 新增 buildLineShift（LCS 行映射），写后诊断对比前先把基线行号按编辑 diff 平移，中部插行不再误报假新错误。
- **第十一轮（0.1.12）** PTY helper 判活修正：修复 protocol.rs 缺失 #[test] 导致 roundtrips_output_with_binary_payload 永不运行；非 PID 判活（socket + Auth + Pong 形状校验）已确认。
- **第十轮（0.1.11）** 发布自动化：`scripts/release.mjs` 一键发布（CHANGELOG 门禁 → 四文件版本递增 → `[未发布]` 固化 → commit → tag vX.Y.Z）；`verify.ps1` 增加 CHANGELOG `[未发布]` 段非空门禁。
- **第十轮（0.1.11）** PTY helper 进程（进程级会话恢复 I1c）：detached 进程持有 portable-pty 会话，TCP 127.0.0.1 + 随机 token 认证，长度前缀帧协议；每会话环形输出缓冲 + 重连回放；主进程 helper 代理连接（`pty_helper_open/attach/write/resize/close/list`），前端新终端默认走 helper（失败自动降级进程内路径）；`attach` 经 Replay 帧回放既有会话；主进程退出发 Shutdown，helper 孤儿超时（10 分钟无客户端）自动退出。
- **第十轮（0.1.11）** 终端 buffer 快照回放（I1c 轻量路径 / helper 降级层）：空闲终端定期 + 关闭时序列化 buffer 到 `~/.yamet/sessions/<leafId>.snap`，重启激活冷标签时先回放上次会话输出再以原 cwd 新起 shell；前台任务 / TUI（alt-screen）运行中不落快照。
- **第十轮（0.1.11）** AI 工具 LSP 语义诊断反馈：`write_file` / `edit` / `multi_edit` 写后主动通知语言服务器（full-text didSave / didOpen）并拉取诊断，只报本次编辑新增项（编辑前基线 diff，freshness 门控），LSP 不可用时静默降级。
- **第十轮（0.1.11）** LSP 跨文件 workspace edits：F2 重命名 / code action 返回的跨文件 edits 不再静默丢弃，实际写入目标文件并通知服务器。
- **第十轮（0.1.11）** LSP WSL 工作区支持：移除 `lsp_spawn` 的 WSL 拒绝，服务器经 `wsl.exe -d <distro> --cd <root> --` 桥接在发行版内运行；`lsp_resolve_root` 增加 WSL 分支（每级一次 `wsl test -e` 参数化检查，无 shell 注入面）；前端 WSL 工作区可启用 LSP。
- **第十轮（0.1.11）** skill bundle 分享：内置/自建 skill 导出为 skill.json（复制到剪贴板），粘贴导入经校验写入 `skills/<name>.json`（同名拒绝覆盖，导入后自动重扫）。
- **第十轮（0.1.11）** mcp/skill i18n 键组彻底拆分：删除历史遗留 `skillsMcp` 混合键组，skill 键并入 `skills` 组、mcp 键并入 `mcp` 组，两个设置组件引用全部更新。
- **第十轮（0.1.11）** SSH 后续（后端）：SFTP 远程浏览（`sftp` 批命令 `ls -la`/`get`,argv 传参无 shell 注入，`ls` 行解析纯函数已测）；`ssh -N -L/-R` 端口转发隧道（start/list/kill，组件清理校验同 target.rs）。前端浏览面板与隧道 UI 待接线。
- **第十轮（0.1.11）** 终端快照回放标注：前台任务运行中退出时写入 busy 标记，恢复的标签显示「上次会话前台任务未保存」提示。
- **第十轮（0.1.11）** IDE 全项目搜索面板（E1）：侧栏新增「搜索」视图，复用 `fs_grep_interactive` 跨文件全文搜索，按文件分组 + 命中高亮 + 点击跳行；替换输入框支持全部替换（逐文件大小写敏感替换，跳过不可读写文件）。
- **第九轮（0.1.10）** 右下角 AgentSwitcher 合并 agent + model 选择器：输入框旁和底部状态栏的模型/agent 选项卡移除，右下角一个下拉同时切换 agent 和 model。
- **第九轮（0.1.10）** 设置页"技能"与"MCP"拆分为独立标签（SkillsSection + McpSection）。
- **第九轮（0.1.10）** 工作区配置持久化：用户选择的工作区根目录经 localStorage 持久化，配置到其它盘后重启不再回退到默认 C 盘用户目录。
- **第九轮（0.1.10）** 自动批准移到批准弹窗：批准框"记住"下拉新增"自动批准此工具"选项，移除对话页顶部的自动批准开关。

### 修复
- **孤儿 tool-call 修复（0.1.23 后，不递增版本）**：修复"an assistant message with tool calls must be followed by tool messages responding to each tool call id"provider 拒绝。根因：一次 run 在工具结果折回消息前结束（用户中止/步数上限/错误）时，AI SDK 会把含 `tool-call` part 但无对应 `tool-result` part 的 assistant 消息固化进历史；下一轮发送该历史时 OpenAI 兼容 API 校验失败。修复：`transport.ts` 新增 `sanitizeOrphanToolCalls`（导出可测），在发送边界剥离孤儿 `tool-call` part（保留同消息文本、丢弃清空后的 assistant 消息），保证出站历史始终良构。6 个测试（良构保留/孤儿剥离/保留文本/清空丢弃/user 不动/多 tool 混合）。tsc 0 + vitest 1897 全绿。
- **第九轮（0.1.10）** 工作区换盘无效（home 用 useState 不持久化，重启回 C 盘）。



### 新增
- **第十八轮（0.1.19）** 剩余 deferred 全量实现：helper PTY 滚动缓冲（HelperSessionHandlers 加 RollingBuffer + Frame::Output 镜像 + pty_helper_buffer_lines 分页命令，闭合第十四轮 T 的 helper 路径缺口）；useSourceControlPanel（1237 行）补测 30 个 + useTerminalSession（1220 行）补测 22 个（Coverage 两个最大 P0 文件零测试缺口闭合）；工作区最近打开（pushRecent + recentSpaces 纯函数 + SpaceSwitcher 渲染最近列表）。1621 前端测试 + cargo clippy 全绿。
- **第十七轮（0.1.18）** 全量补全：远程 SFTP 编辑回写（sftp_write 后端 + RemoteEditorDialog，闭合审计远程只读缺口）；统一 EmptyState（MCP/网关侧栏引导空态）；迷你窗 P1-a 边缘吸附（snapGeom 距边16px自动吸附）+ 双击最大化/还原 + P1-b 置顶 pin（z-40→z-50）；MCP 添加失败 toast 反馈；DAP 适配器缺失错误 banner；git-history 侧栏入口；会话导出全部（Markdown 下载）+ 清空全部按钮。闭合第十五轮 P1/P2 需求、audit-user-ux/audit-frontend 产品缺陷。1557 前端测试 + cargo clippy 全绿。
- **第十六轮（0.1.17）** 平台抽象层迁移（GUI/WebUI/TUI/CLI 共享核心逻辑）：全量收敛 `@tauri-apps/*` 直接引用到 `@/platform` 统一适配器层（136 处归零，仅 `platform/tauri/` 自身保留）——核心 IPC（invoke/Channel/convertFileSrc + 新增 invokeRaw 支持 PTY raw-body 写）、事件（listen/emit/UnlistenFn）、存储（LazyStore→createStorage，autoSave 语义由 adapter.set 自动 save 保证）、窗口/路径/开盖/os/app/process/dialog/notification/autostart/updater/clipboard 便捷函数，每个未初始化回退 Tauri 原生（测试 mock 生效）。架构：`types.ts` 16 接口 + `tauri/` 16 适配器 + `web/` 实现，`detectPlatform()` 按运行时选适配器。1557 前端测试全绿 + cargo clippy 通过。
- **第十五轮（0.1.16）** AI 聊天窗口补强：主窗口会话历史+新建(SessionBar 顶部栏含+新建+下拉切换/重命名/删除); 主窗口 Todo 展示(AiChatPanel 底部 TodoStrip, 与迷你窗共享 todoStore); Edit/Write 工具完成后可折叠展开查看改动内容(旧/新编辑内容); 迷你窗子 agent 进程视图(ActivityStrip 卡片化显示 subagent 类型/实时 step/summary/耗时); 迷你窗内嵌输入框(AiComposerInput 复用顶层 composer); 会话切换草稿保持(composer 按 sessionId 分键保存/恢复)。
- **第九轮（0.1.10）** 汉化收官：34 处显示文本 + 57 处属性硬编码英文全部走 i18n（仅剩白名单：shadcn 原语 / 品牌名 / 示例值 / 协议名）；新增 common.block / gateway.relogin / ai.emptyOutput / ai.resumeTurn / ai.editMessage / git.binary 等键（zh/en 双语）。
- **第九轮（0.1.10）** 锁中毒自愈：152 处 Mutex/RwLock `.unwrap()/.expect()`（含 read/write 与多行形态）改为 `.unwrap_or_else(|e| e.into_inner())`；新增 `src-tauri/tests/lock_poison.rs` 自愈单测；`scripts/verify.ps1` 加 lock poison 门禁。
- **第九轮（0.1.10）** 后台进程树杀：`bash_bg_*` kill 杀整棵进程树（Windows Job Object + Unix 进程组 `process_group(0)`）；补 Unix 组杀测试与 `tests/shell_background_windows.rs`。
- **第九轮（0.1.10）** 崩溃恢复：会话记录 incompleteTurn 标记，流式回合中断后重启，AI 面板显示「继续」入口一键续接。
- **第九轮（0.1.10）** 消息编辑/重做：末轮用户消息可编辑，保存后截断尾部并重发。
- **第九轮（0.1.10）** WeixinReloginOverlay 组件测试：QR 渲染 / scanned 状态 / confirmed 持久化 / 非微信忽略四分支。
- **第八轮（0.1.9）** 微信会话自动重连：会话过期后自动推送重登 QR 到前端（不再暂停 10 分钟），扫码确认后自动更新 token 恢复 poll。
- **第八轮（0.1.9）** 网关限流后置：已授权会话消息不再被限流丢弃（DM auto-trust + 手动批准会话突发消息全送达）。
- **第八轮（0.1.9）** 微信/QQ/Wecom 媒体下载：adapter 轮询循环自动下载图片/文件到 `~/.yamet/media/`，填充 `local_path` 给 agent 使用。
- **第八轮（0.1.9）** 主界面重登 QR 浮层：`WeixinReloginOverlay` 全局监听 `gateway-platform-event`，会话过期自动弹非阻塞浮层。
- **第八轮（0.1.9）** 模型选择器默认过滤无 key 模型：「全部」tab 默认只显示有 API key 的模型，provider 侧栏加"显示未配置"切换。
- **第八轮（0.1.9）** 汉化补全：33 个中文翻译键（ai/explorer/editor/source-control/preview 等）。
- **第八轮（0.1.9）** 自动更新端点配置：`tauri.conf.json` updater.endpoints 填入 GitHub Releases 模板（用户配仓库后替换）。

### 修复
- **第八轮（0.1.9）** P3 授权恢复核查：确认 `set_persist_path` 已内含 `load_from` 恢复，无需改动（审查修正）。

### 文档
- **第八轮（0.1.9）** ROADMAP 测试覆盖扩展已勾选。

### 新增
- **第十八轮（0.1.19）** 剩余 deferred 全量实现：helper PTY 滚动缓冲（HelperSessionHandlers 加 RollingBuffer + Frame::Output 镜像 + pty_helper_buffer_lines 分页命令，闭合第十四轮 T 的 helper 路径缺口）；useSourceControlPanel（1237 行）补测 30 个 + useTerminalSession（1220 行）补测 22 个（Coverage 两个最大 P0 文件零测试缺口闭合）；工作区最近打开（pushRecent + recentSpaces 纯函数 + SpaceSwitcher 渲染最近列表）。1621 前端测试 + cargo clippy 全绿。
- **第十七轮（0.1.18）** 全量补全：远程 SFTP 编辑回写（sftp_write 后端 + RemoteEditorDialog，闭合审计远程只读缺口）；统一 EmptyState（MCP/网关侧栏引导空态）；迷你窗 P1-a 边缘吸附（snapGeom 距边16px自动吸附）+ 双击最大化/还原 + P1-b 置顶 pin（z-40→z-50）；MCP 添加失败 toast 反馈；DAP 适配器缺失错误 banner；git-history 侧栏入口；会话导出全部（Markdown 下载）+ 清空全部按钮。闭合第十五轮 P1/P2 需求、audit-user-ux/audit-frontend 产品缺陷。1557 前端测试 + cargo clippy 全绿。
- **第十六轮（0.1.17）** 平台抽象层迁移（GUI/WebUI/TUI/CLI 共享核心逻辑）：全量收敛 `@tauri-apps/*` 直接引用到 `@/platform` 统一适配器层（136 处归零，仅 `platform/tauri/` 自身保留）——核心 IPC（invoke/Channel/convertFileSrc + 新增 invokeRaw 支持 PTY raw-body 写）、事件（listen/emit/UnlistenFn）、存储（LazyStore→createStorage，autoSave 语义由 adapter.set 自动 save 保证）、窗口/路径/开盖/os/app/process/dialog/notification/autostart/updater/clipboard 便捷函数，每个未初始化回退 Tauri 原生（测试 mock 生效）。架构：`types.ts` 16 接口 + `tauri/` 16 适配器 + `web/` 实现，`detectPlatform()` 按运行时选适配器。1557 前端测试全绿 + cargo clippy 通过。
- **第十五轮（0.1.16）** AI 聊天窗口补强：主窗口会话历史+新建(SessionBar 顶部栏含+新建+下拉切换/重命名/删除); 主窗口 Todo 展示(AiChatPanel 底部 TodoStrip, 与迷你窗共享 todoStore); Edit/Write 工具完成后可折叠展开查看改动内容(旧/新编辑内容); 迷你窗子 agent 进程视图(ActivityStrip 卡片化显示 subagent 类型/实时 step/summary/耗时); 迷你窗内嵌输入框(AiComposerInput 复用顶层 composer); 会话切换草稿保持(composer 按 sessionId 分键保存/恢复)。
- **第七轮（0.1.8）** 反向 MCP server：`src-tauri/src/modules/mcp_server/`（JSON-RPC 2.0 stdio + 6 只读工具 read_file/list_directory/grep/glob/git_status/git_diff + 路径沙箱 + 1MiB 读取上限 + grep 排除 .git），CLI 入口 `yamet __mcp_server`，外部 agent（Claude Code / OpenCode）经 mcpServers 接入。
- **第七轮（0.1.8）** 跨会话语义检索：`search_memories(query)` 工具（★ H1），匹配历史会话 + 项目记忆，摘要注入上下文，纯函数已测。
- **第七轮（0.1.8）** cron 定时自动化：`src-tauri/src/modules/scheduler/`（★ H3，自实现 5 字段 cron + 30s tick + 持久化），前端 `yamet:scheduler-fire` 监听 spawn agent，设置页定时任务区（增删改/启停/下次触发预览）。
- **第七轮（0.1.8）** skills 自动沉淀：`create_skill(name, prompt, toolAllowlist?, handle?)` 工具（★ H2，走审批），写入 `skills/<name>/skill.json` 并刷新内置列表。
- **第七轮（0.1.8）** 全量测试覆盖提升：补测 sessions/todos/slashCommands/memoryStore/utils，覆盖率 11.76% → 31.65% 语句。

### 新增
- **第十八轮（0.1.19）** 剩余 deferred 全量实现：helper PTY 滚动缓冲（HelperSessionHandlers 加 RollingBuffer + Frame::Output 镜像 + pty_helper_buffer_lines 分页命令，闭合第十四轮 T 的 helper 路径缺口）；useSourceControlPanel（1237 行）补测 30 个 + useTerminalSession（1220 行）补测 22 个（Coverage 两个最大 P0 文件零测试缺口闭合）；工作区最近打开（pushRecent + recentSpaces 纯函数 + SpaceSwitcher 渲染最近列表）。1621 前端测试 + cargo clippy 全绿。
- **第十七轮（0.1.18）** 全量补全：远程 SFTP 编辑回写（sftp_write 后端 + RemoteEditorDialog，闭合审计远程只读缺口）；统一 EmptyState（MCP/网关侧栏引导空态）；迷你窗 P1-a 边缘吸附（snapGeom 距边16px自动吸附）+ 双击最大化/还原 + P1-b 置顶 pin（z-40→z-50）；MCP 添加失败 toast 反馈；DAP 适配器缺失错误 banner；git-history 侧栏入口；会话导出全部（Markdown 下载）+ 清空全部按钮。闭合第十五轮 P1/P2 需求、audit-user-ux/audit-frontend 产品缺陷。1557 前端测试 + cargo clippy 全绿。
- **第十六轮（0.1.17）** 平台抽象层迁移（GUI/WebUI/TUI/CLI 共享核心逻辑）：全量收敛 `@tauri-apps/*` 直接引用到 `@/platform` 统一适配器层（136 处归零，仅 `platform/tauri/` 自身保留）——核心 IPC（invoke/Channel/convertFileSrc + 新增 invokeRaw 支持 PTY raw-body 写）、事件（listen/emit/UnlistenFn）、存储（LazyStore→createStorage，autoSave 语义由 adapter.set 自动 save 保证）、窗口/路径/开盖/os/app/process/dialog/notification/autostart/updater/clipboard 便捷函数，每个未初始化回退 Tauri 原生（测试 mock 生效）。架构：`types.ts` 16 接口 + `tauri/` 16 适配器 + `web/` 实现，`detectPlatform()` 按运行时选适配器。1557 前端测试全绿 + cargo clippy 通过。
- **第十五轮（0.1.16）** AI 聊天窗口补强：主窗口会话历史+新建(SessionBar 顶部栏含+新建+下拉切换/重命名/删除); 主窗口 Todo 展示(AiChatPanel 底部 TodoStrip, 与迷你窗共享 todoStore); Edit/Write 工具完成后可折叠展开查看改动内容(旧/新编辑内容); 迷你窗子 agent 进程视图(ActivityStrip 卡片化显示 subagent 类型/实时 step/summary/耗时); 迷你窗内嵌输入框(AiComposerInput 复用顶层 composer); 会话切换草稿保持(composer 按 sessionId 分键保存/恢复)。
- **第六轮（0.1.7）** MCP client：`src-tauri/src/modules/mcp/`（stdio / HTTP 传输 + JSON-RPC 2.0 + 断线重连 + 并发上限 + stderr 环形尾），5 个命令注册；前端动态工具注册（全部 `needsApproval: true` + `redactSensitive` 脱敏 + 工具卡 `mcp · <server>` 来源分支）。
- **第六轮（0.1.7）** Skill 升级：snippet 支持 `toolAllowlist`（技能限定工具回合，`filterTools` 纯函数）、内置 `skills/` 目录约定（`scanSkillsDir` 启动扫描，builtin 可禁用）、设置页工具白名单多选。
- **第六轮（0.1.7）** 记忆增强：`ProjectMemoryEntry.source` 来源分组（tool/auto）、`list_project_memory` / `delete_project_memory` 工具、系统提示尾 nudge、设置页项目记忆浏览/编辑区块。
- **第六轮（0.1.7）** 设置页新增「技能与 MCP」标签（`skillsMcp` 键组，zh/en），MCP 服务器增删改 + 连接/断开 + 工具数展示。
- AI 工具 `update_project_memory`：两级项目记忆（会话内 store + YAMET.md 落盘），完成 P2-9 写路径。
- `scripts/version-bump.mjs`：同步四个文件的版本号（四处同步）。
- `scripts/verify.ps1`：一次性前后端验证门禁（`pnpm verify`：check-types、lint、测试、size、cargo check+test、tauri build）。
- `modelCache` LRU 上限（24），避免长会话累积模型实例。
- 终端粘贴大于 32KiB 时改为分块写入，保持 UI 线程响应。
- 网关连接重入守卫（每平台不产生重复入站循环）。
- 钉钉 / 飞书出站调用复用共享 HTTP client。
- 主题导入遇到 id 冲突时明确提示，而非静默覆盖。
- 图片/PDF 文件预览支持缩放。
- LSP code action（客户端能力声明 + `textDocument/codeAction`）。
- git stash 管理（`git_stash_save/list/pop/apply/drop`）。
- git 冲突解决（`git_merge_abort`，`--ours`/`--theirs` checkout）。
- git 分支管理（创建/删除/重命名），无上游分支的 `push --set-upstream`，pull 策略（`--ff-only` / `--rebase` / `--no-rebase`）。
- git 子模块 status/update 命令。
- AI 工具 `apply_patch`：多文件 unified diff，原子化逐块应用。
- 审批决定可被记住（本会话 / 本项目），以及按工具的拒绝黑名单。
- 会话重命名 UI（会话选择器中的行内重命名）。
- 斜杠命令 `/review`、`/commit`、`/test`、`/fix`（send-prompt 配方）。
- 工作区「打开文件夹」入口：命令面板选目录，授权后将工作区重置到该目录。
- AI 会话历史面板：完整会话列表，支持搜索、按天分组、行内重命名与删除。

### 变更
- **第六轮（0.1.7）** 设置页片段编辑器从「智能体」区迁至「技能与 MCP」区（agents 卡片保留在智能体区）；`TOOL_REGISTRY` 抽为轻量模块，设置窗口不再急切拉取 AI 工具栈。
- 移除 6 个未使用的 `@ai-sdk/{anthropic,cerebras,google,groq,openai,xai}` 依赖（knip 确认为死依赖，应用只使用 `@ai-sdk/openai-compatible`）。

## [0.1.5] — 2026-08-04

### 新增
- **第十八轮（0.1.19）** 剩余 deferred 全量实现：helper PTY 滚动缓冲（HelperSessionHandlers 加 RollingBuffer + Frame::Output 镜像 + pty_helper_buffer_lines 分页命令，闭合第十四轮 T 的 helper 路径缺口）；useSourceControlPanel（1237 行）补测 30 个 + useTerminalSession（1220 行）补测 22 个（Coverage 两个最大 P0 文件零测试缺口闭合）；工作区最近打开（pushRecent + recentSpaces 纯函数 + SpaceSwitcher 渲染最近列表）。1621 前端测试 + cargo clippy 全绿。
- **第十七轮（0.1.18）** 全量补全：远程 SFTP 编辑回写（sftp_write 后端 + RemoteEditorDialog，闭合审计远程只读缺口）；统一 EmptyState（MCP/网关侧栏引导空态）；迷你窗 P1-a 边缘吸附（snapGeom 距边16px自动吸附）+ 双击最大化/还原 + P1-b 置顶 pin（z-40→z-50）；MCP 添加失败 toast 反馈；DAP 适配器缺失错误 banner；git-history 侧栏入口；会话导出全部（Markdown 下载）+ 清空全部按钮。闭合第十五轮 P1/P2 需求、audit-user-ux/audit-frontend 产品缺陷。1557 前端测试 + cargo clippy 全绿。
- **第十六轮（0.1.17）** 平台抽象层迁移（GUI/WebUI/TUI/CLI 共享核心逻辑）：全量收敛 `@tauri-apps/*` 直接引用到 `@/platform` 统一适配器层（136 处归零，仅 `platform/tauri/` 自身保留）——核心 IPC（invoke/Channel/convertFileSrc + 新增 invokeRaw 支持 PTY raw-body 写）、事件（listen/emit/UnlistenFn）、存储（LazyStore→createStorage，autoSave 语义由 adapter.set 自动 save 保证）、窗口/路径/开盖/os/app/process/dialog/notification/autostart/updater/clipboard 便捷函数，每个未初始化回退 Tauri 原生（测试 mock 生效）。架构：`types.ts` 16 接口 + `tauri/` 16 适配器 + `web/` 实现，`detectPlatform()` 按运行时选适配器。1557 前端测试全绿 + cargo clippy 通过。
- **第十五轮（0.1.16）** AI 聊天窗口补强：主窗口会话历史+新建(SessionBar 顶部栏含+新建+下拉切换/重命名/删除); 主窗口 Todo 展示(AiChatPanel 底部 TodoStrip, 与迷你窗共享 todoStore); Edit/Write 工具完成后可折叠展开查看改动内容(旧/新编辑内容); 迷你窗子 agent 进程视图(ActivityStrip 卡片化显示 subagent 类型/实时 step/summary/耗时); 迷你窗内嵌输入框(AiComposerInput 复用顶层 composer); 会话切换草稿保持(composer 按 sessionId 分键保存/恢复)。
- 第四轮迭代：git 分支状态栏徽标、编辑器右键菜单、文件浏览器多选、图片/PDF 文件预览、补全失败反馈 + 自动降级、终端路径补全的 `~` 展开、终端历史持久化到 `~/.yamet/history`、项目记忆写入工具。

## [0.1.4] — 2026-08-03

### 新增
- **第十八轮（0.1.19）** 剩余 deferred 全量实现：helper PTY 滚动缓冲（HelperSessionHandlers 加 RollingBuffer + Frame::Output 镜像 + pty_helper_buffer_lines 分页命令，闭合第十四轮 T 的 helper 路径缺口）；useSourceControlPanel（1237 行）补测 30 个 + useTerminalSession（1220 行）补测 22 个（Coverage 两个最大 P0 文件零测试缺口闭合）；工作区最近打开（pushRecent + recentSpaces 纯函数 + SpaceSwitcher 渲染最近列表）。1621 前端测试 + cargo clippy 全绿。
- **第十七轮（0.1.18）** 全量补全：远程 SFTP 编辑回写（sftp_write 后端 + RemoteEditorDialog，闭合审计远程只读缺口）；统一 EmptyState（MCP/网关侧栏引导空态）；迷你窗 P1-a 边缘吸附（snapGeom 距边16px自动吸附）+ 双击最大化/还原 + P1-b 置顶 pin（z-40→z-50）；MCP 添加失败 toast 反馈；DAP 适配器缺失错误 banner；git-history 侧栏入口；会话导出全部（Markdown 下载）+ 清空全部按钮。闭合第十五轮 P1/P2 需求、audit-user-ux/audit-frontend 产品缺陷。1557 前端测试 + cargo clippy 全绿。
- **第十六轮（0.1.17）** 平台抽象层迁移（GUI/WebUI/TUI/CLI 共享核心逻辑）：全量收敛 `@tauri-apps/*` 直接引用到 `@/platform` 统一适配器层（136 处归零，仅 `platform/tauri/` 自身保留）——核心 IPC（invoke/Channel/convertFileSrc + 新增 invokeRaw 支持 PTY raw-body 写）、事件（listen/emit/UnlistenFn）、存储（LazyStore→createStorage，autoSave 语义由 adapter.set 自动 save 保证）、窗口/路径/开盖/os/app/process/dialog/notification/autostart/updater/clipboard 便捷函数，每个未初始化回退 Tauri 原生（测试 mock 生效）。架构：`types.ts` 16 接口 + `tauri/` 16 适配器 + `web/` 实现，`detectPlatform()` 按运行时选适配器。1557 前端测试全绿 + cargo clippy 通过。
- **第十五轮（0.1.16）** AI 聊天窗口补强：主窗口会话历史+新建(SessionBar 顶部栏含+新建+下拉切换/重命名/删除); 主窗口 Todo 展示(AiChatPanel 底部 TodoStrip, 与迷你窗共享 todoStore); Edit/Write 工具完成后可折叠展开查看改动内容(旧/新编辑内容); 迷你窗子 agent 进程视图(ActivityStrip 卡片化显示 subagent 类型/实时 step/summary/耗时); 迷你窗内嵌输入框(AiComposerInput 复用顶层 composer); 会话切换草稿保持(composer 按 sessionId 分键保存/恢复)。
- 第三轮迭代：AI 工具三件套（终端驱动、文件管理、git）、网关可用性（回调地址、白名单持久化、iLink 重新登录二维码）、Rust FS 工作区授权、扩展 shell 拒绝名单、stash / 冲突解决 / 分支管理 / 子模块、编辑器 code action、quick fix、斜杠命令、会话重命名、多选、历史持久化。

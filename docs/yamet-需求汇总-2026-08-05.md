# Yamet 需求汇总（2026-08-05 更新）

> 汇总第八、九轮及新增调研项。标注每项状态：✅ 已交付 / 🔄 待实施 / ⏸ 评审后定 / ⚠️ 需配置。
> 当前版本 **0.1.10**（第九轮已交付构建）。

---

## 一、第八轮（已交付 ✅ 0.1.9）

### P1 · 微信会话自动重连 ✅
- 会话过期自动推送重登 QR（不再暂停 10 分钟），扫码确认自动更新 token 恢复 poll
- `weixin.rs` relogin 分支 + `Inner.event_sink`

### P2 · 网关限流后置 ✅
- 令牌桶移到授权判定后，已授权会话（含微信 DM auto-trust）突发消息不被丢弃

### P3 · 会话授权状态启动恢复 ✅（核查修正）
- `set_persist_path` 内部已 `load_from` 恢复，**无需改动**（审查修正原"启动不恢复"判断）

### P6 · 汉化补全 ✅
- 33 个中文翻译键 + en 补全

### P7 · 微信重登 QR 主界面感知 ✅
- `WeixinReloginOverlay` 全局监听 platform-event，会话过期自动弹浮层
- **后续修复**：confirmed 态显示 + 测试 beforeEach（提交 `2e9ab14`）

### P8 · 自动更新接通 ⚠️（半完成）
- `tauri.conf.json` updater.endpoints 填 GitHub Releases 占位 URL
- **待用户配置**：远程仓库 + 替换 `{owner}/{repo}` + `GITHUB_LATEST_RELEASE`

### P9 · 微信/QQ/wecom/公众号媒体下载 ✅
- `media.rs` 下载辅助 + 4 个 adapter poll 循环填充 `local_path`

### P10 · ROADMAP 同步 ✅
- 测试覆盖扩展已勾选

### P11 · 模型选择器只显示有 key 模型 ✅
- 「全部」tab 默认过滤无 key 模型，免 key（llama.cpp/compat）恒显示，侧栏加"显示未配置"切换

---

## 二、第九轮（已交付 ✅ 0.1.10）

### N1 · 右下角合并 Agent + Model 选择器 ✅
- 右下角 AgentSwitcher 改为 agent+model 合一选择器（下拉含 agent 分组 + model 分组，模型支持搜索/有 key 过滤）
- 移除输入框旁 2 个 + 底部状态栏 ModelDropdown
- **位置修正**（用户要求）：模型/智能体选择器移到**主窗口底部状态栏**（AiStatusBarControls），折叠窗口（AiMiniWindow）与主聊天面板顶部移除（`cd2ba18`）
- 触发按钮显示 `agent名 · 模型名`
- **后续修复**：主聊天面板（AiChatPanel）顶部加回选择器（`e42ad8a`）；isMiniWindow 模式模型名可见（`04c2cd9`）

### N2 · 设置页技能与 MCP 拆分为独立标签 ✅
- `SkillsMcpSection` 拆分为 `SkillsSection` + `McpSection`
- 补 i18n 键 + 路由（settings.skills / settings.mcp / mcp.title）
- **后续修复**：技能页标题"技能与 MCP"→"技能"（`74ed62f`）

---

## 三、新增调研项（已交付 ✅ 0.1.10）

### N3 · 工作区配置到其它盘无效（真实 bug）✅
- **根因**：`useWorkspaceSwitcher.ts:39` `home` 用 `useState` 保存，**不持久化**；重启重置为 OS `homeDir()`（C 盘）
- **方案已实施**：设置页新增"工作区根目录"配置项（选择文件夹 + 重置），持久化到 LazyStore（`workspaceRoot`）；启动读 LazyStore 优先于 `homeDir()`（`91b5c89`）

### N4 · 自动批准改到批准弹窗 ✅
- **需求**：移到弹出的批准框（AiToolApproval）作为一项选项，不能在对话页面
- **已实施**：AiToolApproval"记住"下拉加"自动批准此工具"选项；移除对话页顶部 Switch

---

## 四、汉化补全（0.1.10 追加）

### 智能体 ✅
- 5 个内置 agent 名（编码助手/架构师/代码审查/安全专家/设计师）+ 描述
- `agentDisplayName`/`agentDisplayDescription` 辅助函数

### 快捷键页 ✅
- 44 项 label + 9 个分组（通用/标签页/面板/终端/工作区/搜索/AI/视图/编辑器）

### 命令面板 ✅
- 23 个命令项 + 8 个分组标题

### 主题页 ✅
- 15 个内置主题显示名（专有配色名保留英文）

---

## 五、版本与优先级（最终）

| 项 | 版本 | 状态 |
|----|------|------|
| P1-P3/P6-P11 | 0.1.9 | ✅ 已交付 |
| N1-N4 | 0.1.10 | ✅ 已交付 |
| 汉化补全（智能体/快捷键/命令面板/主题） | 0.1.10 | ✅ 已交付 |
| P8 自动更新 | 待配置 | ⚠️ 需用户配置仓库 |
| P4 网关独立推理 | — | ⏸ 评审后定 |

---

## 六、验收总门禁（第九轮）

- [x] N1 右下角 agent+model 合一；输入框旁/底部状态栏无 ModelDropdown
- [x] N2 设置页技能/MCP 独立标签
- [x] N3 工作区配置到其它盘重启后仍保留
- [x] N4 自动批准在批准弹窗内，对话页面移除
- [x] `pnpm vitest` 全绿（1001）+ `tsc` 0 错误 + cargo test 全绿（349）
- [x] 构建 0.1.10 替换安装目录

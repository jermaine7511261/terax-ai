# Yamet 深度调研与需求迭代（第二轮 · 2026-08-03）

## 一、上一轮 P0/P1 修复确认（已提交）

| 项 | 状态 | 提交 |
|---|---|---|
| P0-1 网关认证门禁（默认拒绝 + 白名单 + 会话授权 UI） | ✅ | `0ed0f6c` `ff37ad5` |
| P0-2 网关凭据落 keychain（启动恢复） | ✅ | `0ed0f6c` |
| P0-3 shell_run_command 进程树杀（setpgid / Job Object） | ✅ | `0ed0f6c` |
| P0-4 ai_http_request 64MB 上限 + 超时 | ✅ | `0ed0f6c` |
| P1-6 shell-session 失败不永久缓存 | ✅ | `24c8bfb` |
| P1-7 connected 真实状态（不再硬编码 false） | ✅ | `0ed0f6c` |

验证：cargo check 全过 + 前端 509 测试全绿。构建 0.1.1 已完成。

## 二、剩余技术债

1. **P1-5 网关零测试（高风险）**：`crypto.rs`（AES-CBC/ECB、PKCS7、恒定时间比较）、`weixin.rs`（30KB，QR 登录/长轮询/会话过期状态机）无 `#[test]`，违反"核心子系统必须有测试锁定不变量"质量杠。
2. **P0-3 只修了 `run_blocking`**：`shell_session_run`（会话持久 shell）超时同问题未补进程树杀。
3. **文档与代码漂移**：三份 2026-08-03 IM 规划（调研/迭代/国内方案）写"先钉钉逐平台递增版本 0.2.0→0.7.0"，代码实际"一次实现 6 平台 + 认证/持久化"，需对齐；`YAMET.md` 架构章缺 gateway / block 终端 / i18n 三个子系统。

## 三、ROADMAP 已迭代（本次更新）

**移入 Shipped**：block 终端、inline 终端补全（history + AI）、auto-approve、explorer 实时 FS、IM 网关（6 平台 + 认证门禁）。
**Planned 调整**：移除已实现的 history 补全；auto-approve 标已实现（保留 project 策略/per-tool trust）；新增 IM 网关 onboarding/QR 打磨、回调隧道指南、测试覆盖。

## 四、下一批迭代优先级（建议排期）

### P0（安全/正确性，先做）
1. **网关测试**：`crypto.rs`（AES/恒定时间）、`weixin.rs`（iLink 状态机）补 `#[test]` —— 锁定安全不变量
2. **shell_session_run 进程树杀**：对齐 `run_blocking`（setpgid / Job Object）

### P1（工程/产品价值）
3. **布局/会话持久化**：`restoreLayout`/`layoutPersist` 命中 0，重启恢复 tabs/panels/PTY —— 高价值
4. **终端拖放**：`fs_copy` 已铺路（fs/mutate.rs），补前端 drop → 引号路径/AI 上下文
5. **文档对齐**：YAMET.md 补子系统、三份 IM 规划改为现实版本、更新 Wanted contributions（i18n 已完成）

### P2（产品路线）
6. **SSH**（ROADMAP 首项，全库仅 i18n 文案提到，未实现）
7. **编辑器 AI 补全项目感知上下文**（autocomplete 已存在，项目级 context 未做）
8. **AI agent meta-orchestration**（Yamet 编排外部 coding agent CLI：Claude Code / OpenCode）
9. **实时 FS 更新扩展到 editor**（explorer 已接，editor 未做）

## 五、结论

工程地基仍是一流（PTY 背压 + Job Object、net.rs SSRF/DNS 钉扎、security.ts 双层 canonical、LSP 进程组监管），上一轮审计的 P0 安全缺口已全部落地。下一步的投入重心：**① 补齐网关安全逻辑的测试（P0）② 落地会话/布局持久化与终端拖放（高价值产品项）③ 按现实更新文档**。SSH 与外部 agent 编排作为中长期主线。
